# Progress Tracker

Update this file whenever the current phase, active feature, or implementation state changes.

## Current Phase

- Feature: Spec UI Integration (context/feature-specs/29-spec-ui-integration.md) — last of the 27→28→29 spec-generation sequence, implemented in order in one pass.

## Current Goal

- 27/28/29 implemented end to end: spec generation now has the same shape as design generation — `POST /api/ai/spec` → Trigger.dev `generate-spec` task → `POST /api/ai/spec/token`, the task persists its Markdown output to Vercel Blob + a new `ProjectSpec` Prisma row, and the Specs tab lists/previews/downloads real generated specs. Every numbered spec through 29 has been implemented. Nodes render their real shape, support a drag ghost preview, resize handles, inline label editing, a floating color toolbar, custom right-angle edges with hover/selection states and inline labels, a floating zoom/undo-redo control bar with keyboard shortcuts and a collapsible minimap, a starter-template library, live collaborator avatars + cursors (with a thinking spinner) inside the canvas view, a fully wired AI sidebar (AI Architect chat over `ai-chat` with real design-generation triggering + live run tracking, and a Specs tab with a real spec list/preview modal/download action), debounced canvas autosave to Vercel Blob, a design-generation flow (`design-agent` task mutating the shared canvas, `ai-status-feed` progress), and a spec-generation flow (`generate-spec` task producing and persisting a Markdown spec from the canvas graph + chat history). Still no full color picker, no custom/saved user templates, and the Specs tab's "Generate Spec" button is still inert — nothing yet assembles `{chatHistory, nodes, edges}` and calls `POST /api/ai/spec` from the UI.

## Completed

- 01-design-system.md: shadcn/ui initialized (components.json, style base-nova); added button, card, dialog, input, tabs, textarea, scroll-area to `components/ui/*` (unmodified post-install); lucide-react installed; `lib/utils.ts` `cn()` helper generated; `app/globals.css` dark-only palette wired to `context/ui-context.md` tokens (bg-base/surface/elevated/subtle, copy-primary/secondary/muted/faint, brand, accent-dim, ai, ai-text, error/success/warning) and mapped onto shadcn's semantic tokens; `<html>` forced to `dark` class in `app/layout.tsx`. Verified: `tsc --noEmit`, `next build`, `eslint .` all clean; smoke-tested every installed component import + `cn()` then removed the scratch page.
- 02-editor.md: `components/editor/editor-navbar.tsx` — fixed-height (`h-14`) top bar, left/centre/right sections, left holds a sidebar toggle button swapping `PanelLeftOpen`/`PanelLeftClose` on `isSidebarOpen`, right section left empty, `bg-surface` + `border-b border-surface-border`. `components/editor/project-sidebar.tsx` — floating overlay (`absolute`, doesn't push layout), slides in/out via `translate-x` transition on `isOpen`, header with "Project" title + close button, shadcn `Tabs` ("My Projects" / "Shared") each with an empty placeholder state, full-width `New Project` button with `Plus` icon pinned to the bottom. `components/ui/dialog.tsx` restyled to the modal token pattern from `context/ui-context.md` (`rounded-3xl` content/footer, stronger `backdrop-blur-sm` overlay); Title/Description/Footer sub-components already existed from shadcn install and are untouched — no actual dialog instances built yet. Wired `EditorNavbar` + `ProjectSidebar` into `app/page.tsx` as the editor shell with sidebar-open state. Verified: `tsc --noEmit`, `eslint .`, `next build` all clean.
- 03-auth.md: installed `@clerk/ui`. `proxy.ts` (root, Next 16's rename of `middleware.ts`) runs `clerkMiddleware` with a protected-first `createRouteMatcher` built from `NEXT_PUBLIC_CLERK_SIGN_IN_URL`/`NEXT_PUBLIC_CLERK_SIGN_UP_URL` (added to `.env` — Clerk's own conventional var names, not invented) as the only public routes; everything else runs `auth.protect()`. `app/layout.tsx` wraps `{children}` in `ClerkProvider` (inside `<body>`, per current Clerk SDK placement) with `appearance.theme: dark` from `@clerk/ui/themes` and every `appearance.variables` color/font/radius entry pointed at the app's existing `--*` CSS custom properties from `app/globals.css` — no hardcoded colors, including `colorModalBackdrop` via `color-mix()` off `--bg-base`. `components/auth/auth-layout.tsx` — two-panel layout (`grid lg:grid-cols-2`): left panel (`hidden lg:flex`) has a compact `Ghost` logo mark + wordmark, a one-line tagline, and a text-only feature list (no cards, no gradients, no hero); right panel centers the Clerk form; small screens collapse to form-only. `app/sign-in/[[...sign-in]]/page.tsx` and `app/sign-up/[[...sign-up]]/page.tsx` render `<SignIn />`/`<SignUp />` inside `AuthLayout`. Editor content moved from `app/page.tsx` to `app/editor/page.tsx` (unchanged otherwise); `app/page.tsx` is now a server component that awaits `auth()` and redirects — authenticated to `/editor`, unauthenticated to `/sign-in` (the proxy also catches unauthenticated `/` and `/editor` before the page runs, since only sign-in/up are public). `UserButton` added to `EditorNavbar`'s right section (default Clerk menu/profile flows, untouched). Verified: `tsc --noEmit`, `eslint .`, `next build` all clean; smoke-tested with `next dev` — unauthenticated `/` and `/editor` both 307 to `/sign-in`, `/sign-in` renders 200 with the two-panel layout.
- 03-auth.md follow-up (visual pass against a reference screenshot): reworked `components/auth/auth-layout.tsx` to a true 50/50 `lg:grid-cols-2` split with `bg-subtle` on the left panel (vs. `bg-base` on the right) so the two halves read as distinct surfaces; left panel now carries a bold two-line headline, a description paragraph, and an icon-marked feature list (`Sparkles`/`Share2`/`FileText` in `bg-accent-dim` chips — icons only, no bordered/shadowed cards) plus a footer copyright line, replacing the earlier plain-text-only version. Added `components/logo.tsx` (brand-colored mark + "Ghost AI" wordmark) reused for the panel header. Verified visually with a Playwright screenshot of `/sign-in` (Chromium installed ad hoc via `npm install --no-save playwright`, not added as a project dependency) confirming Geist Sans renders throughout, including inside the Clerk widget.
- 04-project-dialogs.md: `types/project.ts` defines `Project` (`id`/`name`/`slug`/`role: "owner" | "collaborator"`); `lib/slug.ts` has a plain `slugify()`; `lib/mock-projects.ts` seeds 3 mock projects (2 owner, 1 collaborator) — no API/persistence anywhere. `hooks/use-project-dialogs.ts` is the single hook the spec asked for: owns the mock `projects` array, current `dialog` (`{type, project?} | null`), the shared `name` form field (+ derived `slugPreview` via `useMemo(slugify)`), and `isLoading`; `open*`/`close` set dialog state, `submit*` simulate a 400ms async call (`setTimeout`) before mutating the in-memory `projects` array and closing. `components/projects/project-dialogs.tsx` renders all three dialogs (Create/Rename/Delete) off one `state` prop from the hook, using the existing `components/ui/dialog.tsx` + newly-added `components/ui/dropdown-menu.tsx` (installed via `npx shadcn add dropdown-menu` — base-ui `Menu` primitive, same render-prop pattern as the existing `Dialog`). Create has a name input with a live `/slug-preview` line below it; Rename prefills the name, `autoFocus`es the input, and shows the current name in the description; both wrap the input in a `<form onSubmit>` so Enter submits. Delete is confirmation-only (no input), its confirm button uses `variant="destructive"`. `app/editor/page.tsx` center content now shows the heading/description/`New Project` button (`Plus` icon) exactly as specced, no cards, wired to `openCreate`; `ProjectDialogs` mounted at the page level. `components/editor/project-sidebar.tsx` (navbar untouched, sidebar open/close/slide behavior untouched per spec) now renders the hook's mock `projects` split into the existing "My Projects"/"Shared" tabs; owned rows get a `MoreVertical` dropdown (Rename/Delete, destructive-styled delete item) revealed on hover, collaborator rows render name-only with no actions; added a `lg:hidden` backdrop scrim (`fixed inset-0 bg-black/50`) behind the sidebar that closes it on click/tap, sidebar `z-40` above the scrim's `z-30`. Verified: `tsc --noEmit`, `eslint .`, `next build` all clean.
- 06-project-apis.md: `app/api/projects/route.ts` - `GET` lists `prisma.project.findMany({ where: { ownerId: userId } })` ordered by `createdAt desc`; `POST` reads the JSON body, defaults a missing/blank `name` to `"Untitled Project"`, and creates with `ownerId: userId` - no explicit `id`, so Prisma's existing `@default(cuid())` strategy is what generates it, per the "do not add sequential IDs" rule. `app/api/projects/[projectId]/route.ts` - `PATCH` renames (400 if the resolved `name` is empty after trim) and `DELETE` removes (204 on success, relying on the schema's existing `onDelete: Cascade` for collaborators); both look the project up first and return `403` if `existing.ownerId !== userId`, `404` if the project doesn't exist. All four handlers call `const { userId } = await auth()` from `@clerk/nextjs/server` and return `401` when it's null - this only works because `proxy.ts`'s public-route matcher was extended with `/api(.*)`, since `clerkMiddleware`'s `auth.protect()` unconditionally 307-redirects unauthenticated requests to `/sign-in` (confirmed by curling the routes pre-fix and getting redirects instead of `401`, even with an `Accept: application/json` header) - API routes now skip that middleware-level protect and enforce their own auth so callers get real `401` JSON instead of an HTML redirect; page routes (`/`, `/editor`) were re-curled afterward and still 307 as before. Followed `node_modules/next/dist/docs/01-app/03-api-reference/03-file-conventions/route.md` for the Next 16 route-handler shape (`Response.json()`, `{ params }: { params: Promise<{ projectId: string }> }`). Backend-only per the spec - no UI wiring. Verified: `tsc --noEmit`, `eslint .`, `next build` all clean; `next dev` smoke test confirmed `401` JSON on all four routes when signed out and unaffected `307` behavior on the page routes.
- 07-wire-editor-home.md: `lib/projects.ts` is the server-only data helper - `getUserProjects(userId, email)` runs owned (`ownerId`) and shared (`collaborators: { some: { email } } }`) queries in parallel via `lib/prisma.ts`, `getProjectForViewer(projectId, userId, email)` scopes a single project to owner-or-collaborator viewers and returns `null` otherwise. `types/project.ts` now holds the real `ProjectSummary` shape (`id`/`name`/`role`) - the old mock `slug` field is gone since the Prisma schema never had one. `app/editor/page.tsx` is now a server component: calls `auth()`/`currentUser()`, fetches both lists through the helper (no client-side fetch on first load), and renders the new `components/editor/editor-shell.tsx` client component with them. Added `app/editor/[projectId]/page.tsx` (new, not in the spec's file list but required for "create navigates to workspace" and "delete redirects to `/editor` if deleting the active workspace" to be real, working behavior rather than dead links) - same data-fetching shape, plus `getProjectForViewer` for access control, `notFound()` if the viewer has no access. `hooks/use-project-actions.ts` replaces the old mock `use-project-dialogs.ts`: create generates a short suffix (`Math.random().toString(36).slice(2, 8)`) on dialog open, derives a `roomIdPreview` via `slugify(name)-suffix`, `POST /api/projects` on submit, then `router.push` to `/editor/${project.id}` - the created project's real Prisma `id` *is* the room id used for navigation (no separate room-id field exists anywhere to reconcile, so "project ID and Liveblocks room ID stay aligned" by construction rather than by a syncing step); rename stores `{id, name}`, `PATCH`es, then `router.refresh()`; delete stores the target project, `DELETE`s, then `router.push("/editor")` if it was the `activeProjectId` passed in from the workspace page, otherwise `router.refresh()`. `components/editor/editor-shell.tsx` (new) centralizes the navbar/sidebar/dialogs wiring that used to live directly in `app/editor/page.tsx`, so both the home and workspace pages share it - takes `ownedProjects`/`sharedProjects` and an optional `activeProject`, rendering the empty-state copy when there's no active project and a placeholder workspace heading (canvas isn't specced yet) when there is. `components/editor/project-sidebar.tsx` now takes `ownedProjects`/`sharedProjects` as two separate props (matching "pass both lists to the sidebar" instead of one array filtered by role client-side) and project names are `next/link`s to `/editor/[id]`. `components/projects/project-dialogs.tsx` swapped its slug-preview copy for the room-id preview/validation text and added an inline error line since dialogs now hit real network calls that can fail. Deleted `hooks/use-project-dialogs.ts` and `lib/mock-projects.ts` (fully superseded). Verified beyond the "Check When Done" list: a throwaway `tsx` script re-ran the exact owned/shared/access-control queries from `lib/projects.ts` against the live database to confirm they return the right rows; a temporary public `/dev-preview` route (removed after, along with its one-line `proxy.ts` addition) rendered `EditorShell` with fixture data and was screenshotted with Playwright - confirmed the sidebar renders real owned/shared projects in their tabs and the create dialog shows `Room: my-cool-project-<suffix>` live as expected. `tsc --noEmit`, `eslint .`, `next build` all clean.
- 08-editor-workspace-shell.md: renamed the dynamic route from `app/editor/[projectId]/` to `app/editor/[roomId]/` per the spec's own naming (`/editor/[roomId]`) - URLs are unaffected since the segment value is still the project's id, only the `params` key name inside the page changed. New `lib/project-access.ts` holds the two helpers the spec named explicitly: `getCurrentIdentity()` (Clerk `userId` + primary email, `null` if signed out) and `checkProjectAccess(projectId, identity)` (owner-or-collaborator check, `null` for both "doesn't exist" and "no access" so callers can't distinguish the two - avoids leaking project existence). This superseded `getProjectForViewer` in `lib/projects.ts`, which was deleted to avoid two competing access-check implementations. `app/editor/[roomId]/page.tsx` now redirects signed-out users to `/sign-in` before anything else, then renders `AccessDenied` inline (not a redirect/404) whenever `checkProjectAccess` returns `null`, covering both missing and unauthorized projects identically per the spec. `components/editor/access-denied.tsx` (new) - full-viewport centered layout, `Lock` icon in a `bg-accent-dim` chip, short message, `Link` back to `/editor`. Layout: `components/editor/editor-navbar.tsx` gained optional `projectName` (centered) and `actions` (right side, before `UserButton`) props rather than a second navbar component, so editor home and the workspace share one implementation; `components/editor/editor-shell.tsx` now passes the active project's name plus a `Share2` button and a `PanelRightOpen`/`PanelRightClose`-toggled AI-sidebar button (both inert beyond that toggle - no share or chat logic, per the spec's explicit scope cut) only when `activeProject` is set. New `components/editor/ai-sidebar.tsx` mirrors `ProjectSidebar`'s floating/slide-over treatment but anchored `right-0`, structural placeholder only ("AI chat is coming soon"). `components/editor/project-sidebar.tsx` gained an `activeProjectId` prop that highlights the current room's row (`bg-elevated` + `font-medium`) in both the owned and shared lists. The center canvas placeholder (`bg-base`, centered "Canvas coming soon" message, `flex-1` so it fills whatever space the two side panels leave) replaces the old plain workspace text. Verified beyond the "Check When Done" list: a throwaway `tsx` script re-ran `checkProjectAccess`'s exact query against the live database for all four cases (owner, collaborator, unrelated stranger, nonexistent project) and got the right `null`/object result each time; a temporary public `/dev-preview` route (removed after, with its one-line `proxy.ts` addition) rendered the workspace shell and `AccessDenied` and was screenshotted with Playwright to confirm the navbar actions, AI sidebar slide-over, sidebar highlighting, and access-denied layout all render as specced. `tsc --noEmit`, `eslint .`, `next build` all clean.
- 09-share-dialog.md: new `types/collaborator.ts` (`CollaboratorSummary`: `id`/`email`/`name`/`imageUrl`, the latter two `null` when unmatched) and `lib/collaborators.ts` (`listCollaborators(projectId)`) - reads `ProjectCollaborator` rows from Postgres (still just `id`+`email`, no local user table per the spec), then does one batch `clerkClient().users.getUserList({ emailAddress: [...] })` call to enrich them with Clerk's `fullName`/`imageUrl`; since that filter is a case-insensitive *partial* match per Clerk's own docs, each returned user's `emailAddresses` is re-checked against the exact wanted set before being trusted as a match, and any email with no Clerk user just carries `name: null, imageUrl: null` through (UI falls back to showing the bare email). New nested routes `app/api/projects/[projectId]/collaborators/route.ts` (`GET` list, `POST` invite) and `.../[collaboratorId]/route.ts` (`DELETE`) all reuse `lib/project-access.ts`'s `getCurrentIdentity`/`checkProjectAccess` from 08 - `401` signed out, `403` non-members, and `403` again on `POST`/`DELETE` for a signed-in collaborator who isn't the owner (enforced server-side, not just hidden in the UI); invite validates the email with a plain regex and catches Prisma's `P2002` on the `(projectId, email)` unique constraint to return `409` for a duplicate invite instead of a 500; delete re-checks the collaborator row's `projectId` matches the route param before deleting, so a collaborator id can't be used to delete a row on a project the caller doesn't own. `hooks/use-share-dialog.ts` fetches the list on dialog open, exposes `invite`/`remove`/`copyLink` (clipboard write + `copied` flag auto-clearing after 2s via `setTimeout`), and needed one `eslint-disable-next-line react-hooks/set-state-in-effect` on the open-triggered fetch - the new stricter hooks lint flags any effect that synchronously calls an in-scope async function starting with a `setState`, but effect-triggered fetch-on-open is React's own documented exception (no server-rendered data to hydrate a client-only dialog from). `components/projects/share-dialog.tsx` renders the invite form only for `role === "owner"` (collaborators get read-only "view the collaborator list" per the spec), a scrollable collaborator list with avatar-or-initial-circle rows and a remove button gated the same way, and a copy-link button that swaps to a checkmark "Copied!" label. `components/editor/editor-shell.tsx`'s previously-inert `Share2` navbar button now opens the dialog, passing `activeProject.role` through so the dialog knows whether to show owner controls. Verified beyond the "Check When Done" list: three throwaway `tsx` scripts hit the real database and Clerk Backend API directly - confirmed the unique-constraint rejection (`P2002`) on a duplicate invite, confirmed an unmatched email correctly returns zero Clerk matches, and confirmed a real Clerk account's email enriches with its actual `imageUrl` (this Clerk instance's test user has no first/last name set, which incidentally proved the `name: null` → email-fallback path too); a temporary public `/dev-preview` route (removed after, with its one-line `proxy.ts` addition) rendered `ShareDialog` standalone and was driven with Playwright (clipboard permissions granted in the browser context) to confirm `Copy link` actually writes `.../editor/<id>` to the clipboard and the button visibly swaps to "Copied!". `tsc --noEmit`, `eslint .`, `next build` all clean.

- 10-liveblocks-setup.md: `liveblocks.config.ts` (root, pre-existing empty template) now declares `Presence` (`cursor: {x,y} | null`, `isThinking: boolean`) and `UserMeta` (`id`, `info: {name, avatar, color}`) per spec; the still-unused `Storage`/`RoomEvent`/`ThreadMetadata`/`RoomInfo` sections keep their template `{}` shape with an inline `eslint-disable` each (repo's `@typescript-eslint/no-empty-object-type` rule newly fires on this file since it wasn't linted/built before). `lib/liveblocks.ts` - cached `Liveblocks` node client (`lib/prisma.ts`'s exact `globalThis` singleton pattern: cache outside production, throw if `LIVEBLOCKS_SECRET_KEY` unset) plus `getCursorColor(userId)`, a simple string-hash → fixed 8-color hex palette mapper (deterministic, not read from `app/globals.css` tokens since this runs server-side outside Tailwind scope). `app/api/liveblocks-auth/route.ts` (new) - `POST`, ID-token auth per Liveblocks' recommended method: `getCurrentIdentity()`/`checkProjectAccess()` from `lib/project-access.ts` (same helpers 08/09 already established) gate on `401` signed-out / `403` no access to the room-id-as-project-id from the request body; `liveblocks.getOrCreateRoom(roomId, { defaultAccesses: ["room:write"] })` creates the room only if missing - `["room:write"]` (not `[]`) because this route is the only place a session token is ever issued and it already gated access above, so trusting any token from this app avoids maintaining a duplicate `usersAccesses` allowlist that invites would need to keep in sync; `liveblocks.identifyUser({userId, groupIds: []}, {userInfo: {name, avatar, color}})` (`groupIds: []` required by the installed `@liveblocks/node@3.24.0`'s `Identity` type, unused otherwise) supplies Clerk's `fullName`/`imageUrl` (falling back to the primary email, then `"Anonymous"`) plus the deterministic cursor color, and the route returns Liveblocks' own `{status, body}` untouched. `@liveblocks/node` was missing from `package.json`/`node_modules` despite the spec's "all required packages are already installed" - installed via `npm install @liveblocks/node` (now `^3.24.0`, matching the other already-installed `@liveblocks/*` packages). `.env` had no `LIVEBLOCKS_SECRET_KEY`; added a placeholder that the user then replaced with a real key from their Liveblocks dashboard. Verified: `tsc --noEmit`, `eslint .`, `next build` all clean (route appears in the build's route list as `ƒ /api/liveblocks-auth`); `next dev` smoke test confirmed a signed-out `POST` returns `401` before any Liveblocks call is made. No UI wiring yet - the spec was infra-only (config, client, auth route); no `RoomProvider`/presence hooks/cursor rendering exist in the editor yet, so nothing in the app actually opens a Liveblocks connection.

- 11-base-canvas.md: `types/canvas.ts` (new) - `NodeShape` union + `NODE_SHAPES` array and `NodeColor`/`NODE_COLORS` (8 fill/text pairs, index 0 default) transcribed verbatim from `context/ui-context.md`'s Canvas section since that doc explicitly names this file as their home, even though nothing renders them yet (scope limit: no custom node/edge rendering); `CanvasNodeData` (`label`/`color`/`shape`) and the `CanvasNode`/`CanvasEdge` custom React Flow types (`Node<CanvasNodeData, "canvasNode">` / `Edge<{}, "canvasEdge">`) the spec named explicitly. `components/editor/canvas.tsx` (new, client) - the actual `<ReactFlow>` render: `useLiveblocksFlow<CanvasNode, CanvasEdge>({suspense: true, nodes: {initial: []}, edges: {initial: []}})` from `@liveblocks/react-flow` feeds `nodes`/`edges`/`onNodesChange`/`onEdgesChange`/`onConnect`/`onDelete` straight into `ReactFlow`, plus `connectionMode={ConnectionMode.Loose}`, `fitView`, `colorMode="dark"`, a dot-pattern `<Background variant={BackgroundVariant.Dots}>`, and `<MiniMap>` - no `<Controls>`, no custom node/edge type components registered (both explicit scope limits). `components/editor/canvas-room.tsx` (new, client) - the Liveblocks room wrapper the spec asked for: `LiveblocksProvider authEndpoint="/api/liveblocks-auth"` → `RoomProvider id={roomId} initialPresence={{cursor: null, isThinking: false}}` → `CanvasErrorBoundary` → `ClientSideSuspense fallback={<CanvasLoading/>}` → `Canvas`; `CanvasLoading` is a small centered `Loader2` spinner + "Loading canvas…" text using the app's existing token classes. `components/editor/canvas-error-boundary.tsx` (new) - a hand-written class-component `ErrorBoundary` (no `react-error-boundary` package - not already installed, and one extra dependency wasn't worth it for a single catch site) rendering a centered `AlertTriangle` + "Couldn't connect to the canvas" message on any error thrown by the Suspense tree below it. `components/editor/editor-shell.tsx`'s `main` now branches: `activeProject` renders `<CanvasRoom roomId={activeProject.id} />` filling the flex-1 area (replacing the old "Canvas coming soon" placeholder text), no-active-project keeps the existing empty-state copy/button unchanged. `app/editor/[roomId]/page.tsx` untouched - already server-side per the spec's step 1, `CanvasRoom` is the client boundary where the actual Liveblocks connection opens. Verified beyond the "Check When Done" list: a temporary public `/dev-preview` route (removed after, with its one-line `proxy.ts` addition) rendered `CanvasRoom` standalone and was driven with Playwright (chromium installed ad hoc via `npm install --no-save playwright`, not added as a project dependency) - confirmed the component tree mounts without a React crash, the `LiveblocksProvider`/`RoomProvider` correctly issue a `POST /api/liveblocks-auth` on connect, and an unauthenticated request's real `401` surfaces as a Liveblocks connection-rejected console error rather than an unhandled exception (expected: this dev-preview route had no signed-in Clerk session, so the `ClientSideSuspense` fallback stayed showing rather than ever reaching an authenticated room - a full connect-and-render check needs a real signed-in session against a real project id, which wasn't set up in this pass). `tsc --noEmit`, `eslint .`, `next build` all clean (confirmed with a `rm -rf .next` rebuild after the dev-preview route was removed, since Next's generated route-validator types briefly referenced the deleted page).

- 12-shape-panel.md: `types/canvas.ts` gained `NODE_DEFAULT_SIZES` (per-shape `{width, height}` - rectangle 160×80 wider-than-tall, circle 100×100 square, diamond 140×140 slightly larger for label room, pill/cylinder/hexagon sized similarly) and `ShapeDragPayload` (`{shape, width, height}`), the drag payload shape the spec asked for. `lib/canvas.ts` (new) - `SHAPE_DRAG_MIME_TYPE` (a custom `application/x-ghost-canvas-shape` dataTransfer type, not `text/plain`, so the canvas's drop handler can't misfire on unrelated drags) and `createNodeId(shape)` (`${shape}-${Date.now()}-${counter}`, an in-memory module-level counter disambiguating same-millisecond drops - exactly "shape name, timestamp, and a counter" per spec). `components/editor/shape-panel.tsx` (new) - the floating pill toolbar (`absolute bottom-4 left-1/2 -translate-x-1/2`, `rounded-full` + `bg-surface/90` + `backdrop-blur-sm`, matching the sidebar/dialog floating-overlay token pattern from `context/ui-context.md`), one `draggable` icon button per shape (`RectangleHorizontal`/`Diamond`/`Circle`/`Pill`/`Cylinder`/`Hexagon` from `lucide-react`) whose `onDragStart` writes a `ShapeDragPayload` (shape + that shape's `NODE_DEFAULT_SIZES`) into `dataTransfer`. `components/editor/canvas-node.tsx` (new) - the `canvasNode` type's basic renderer per spec step 7: every shape (ignoring `data.shape` for now) renders as one bordered `rounded-md` div with the label centered, background/text colors read from `data.color` via a `NODE_COLORS` lookup (falls back to `DEFAULT_NODE_COLOR` for an unrecognized color), plus four `Handle`s (top/right/bottom/left, target/source alternating) so the loose-connection behavior 11-base-canvas.md already wired stays usable on real nodes - not called out explicitly in this spec's steps, but connection handles are structural (not the "shape-specific visuals" the spec deferred) and the canvas had no real nodes to connect until this feature. `components/editor/canvas.tsx` - registers `nodeTypes={{canvasNode: CanvasNodeRenderer}}`, wraps the `<ReactFlow>` in a `relative flex-1` div carrying `onDragOver` (`preventDefault` + `dropEffect: "copy"`) and `onDrop`, and renders `<ShapePanel />` as a sibling overlay inside that div; `onDrop` reads the `ShapePanel`'s payload, converts the drop's screen position to canvas coordinates with `useReactFlow()`'s `screenToFlowPosition`, and calls `useLiveblocksFlow`'s own `onNodesChange([{type: "add", item: newNode}])` (React Flow v12's standard controlled-add pattern) with an empty label and `DEFAULT_NODE_COLOR.fill` per spec step 5 - this is also why `useReactFlow` needed a `ReactFlowProvider` ancestor, which `components/editor/canvas-room.tsx` now wraps around `ReactFlowProvider > ClientSideSuspense > Canvas` (had to be inside the Suspense boundary since `Canvas` renders `ReactFlow` and `screenToFlowPosition` needs `ReactFlow`'s internal store, not just any ancestor provider). Verified beyond the "Check When Done" list: a temporary public `/dev-preview` route (removed after, with its one-line `proxy.ts` addition) rendered `ShapePanel` alongside a plain (non-Liveblocks) `ReactFlow` instance with one `canvasNode`-typed fixture node, screenshotted with Playwright to confirm the pill toolbar, all 6 shape icons, the bordered-rectangle node renderer, and its 4 connection handles all render correctly - the full authenticated drag-and-drop-into-a-real-room path wasn't exercised end-to-end since that needs a signed-in Clerk session, which wasn't set up in this pass (same gap noted in 11-base-canvas.md's verification). `tsc --noEmit`, `eslint .`, `next build` all clean (rebuilt with `rm -rf .next` after removing the dev-preview route, same generated-types staleness as before).
- 12-shape-panel.md follow-up (user-reported bug): every dropped shape rendered as a plain rectangle regardless of which icon was dragged - this was the literal, intended behavior of 12-shape-panel.md's own step 7 ("render every shape as a simple bordered rectangle... shape-specific visuals will be added later"), but the user wanted the real shapes now rather than deferred, so `components/editor/canvas-node.tsx` was rewritten to branch on `data.shape`: rectangle/circle/pill are plain divs using CSS `border-radius` (`0.375rem` / `9999px`), diamond/hexagon are inline SVG `<polygon>`s in a `0 0 100 100` viewBox with `preserveAspectRatio="none"` so they stretch to the node's actual (possibly-resized) box, and cylinder is a two-`<path>` SVG (body + a rim line under the top cap) - matching `context/ui-context.md`'s "complex shapes are rendered as inline SVGs rather than CSS borders" note. Label text is now a separate `pointer-events-none` absolutely-centered overlay (`ShapeLabel`) shared by all shape branches rather than living inside the shape element itself, since SVG text can't be styled/truncated the same way as HTML; the SVGs themselves are also `pointer-events-none` so they don't intercept node-drag or handle-hover events, which only worked because the 4 connection `Handle`s are rendered *after* the SVG in each branch (unstyled `position: absolute` elements with no explicit `z-index` stack in DOM order). Verified with the same temporary `/dev-preview` route + Playwright pattern (removed after): rendered one node per shape side-by-side and screenshotted - all 6 now look visually distinct and correct (rectangle, diamond, circle, pill, cylinder with a lid rim, hexagon). `tsc --noEmit`, `eslint .`, `next build` all clean.
- 13-node-shape.md: step 1 ("replace the placeholder node shape rendering" with CSS shapes for rectangle/pill/circle, SVG for diamond/hexagon/cylinder, scaling with node size, subtle-at-rest/brighter-when-selected borders) was already fully satisfied by the 12-shape-panel.md follow-up above, so this spec's only remaining work was step 2, the shape drag preview. `components/editor/canvas-node.tsx` now exports its shape-geometry constants (`CSS_SHAPE_RADIUS`, `POLYGON_POINTS`, `CYLINDER_BODY_PATH`, `CYLINDER_RIM_PATH`) instead of keeping them file-local, so the preview builder can draw the exact same shapes without duplicating the geometry. `lib/canvas.ts` gained `createShapeDragPreviewElement(shape, width, height)` - builds a detached DOM element (plain `div` with CSS `border-radius` for rectangle/pill/circle, an inline SVG for the rest) at the shape's real `NODE_DEFAULT_SIZES` and `DEFAULT_NODE_COLOR.fill`, i.e. exactly what dropping that shape actually creates. `components/editor/shape-panel.tsx`'s `handleDragStart` appends that element to `document.body`, hands it to `event.dataTransfer.setDragImage(preview, width / 2, height / 2)` (centered under the cursor), then removes it on the next tick - `setDragImage` only needs the element attached long enough for the browser to rasterize it at `dragstart`, after which the browser-drawn image tracks the cursor and disappears on drop/cancel entirely natively, so no manual position-tracking or cleanup-on-drop code was needed. Steps 3/scope limits (shape panel layout, dropped-node creation, resize/label editing) were left untouched. Verified: `tsc --noEmit`, `next build` both clean.
- 14-node-editing.md: `types/canvas.ts` gained `MIN_NODE_WIDTH`/`MIN_NODE_HEIGHT` (60×40) as the floor enforced while resizing. `components/editor/canvas-node.tsx` now renders `<NodeResizer nodeId={id} isVisible={selected} minWidth={MIN_NODE_WIDTH} minHeight={MIN_NODE_HEIGHT} handleStyle={...} lineStyle={...} />` (from `@xyflow/react`) inside every shape branch, styled subtle-and-dark via inline `handleStyle`/`lineStyle` (`var(--color-surface)`/`var(--color-copy-secondary)` tokens) rather than `className` overrides, since `NodeResizer`'s own default handle color is set via inline style and inline `style` is the only way to reliably win that specificity fight over a plain Tailwind class; `NodeResizer` dispatches resize as `dimensions` node changes through React Flow's internal `triggerNodeChanges`, which - same as the built-in drag/select changes already flowing through `onNodesChange` since 11-base-canvas.md - routes straight into `useLiveblocksFlow`'s controlled `onNodesChange` with no new wiring needed. Inline label editing: the previous `ShapeLabel` (a `pointer-events-none` centered span) is now wrapped in a new plain (pointer-events default) `labelOverlay` div carrying `onDoubleClick` so the whole label/center area of every shape opens editing, `ShapeLabel` itself now shows italic/dimmed "Double-click to add a label" placeholder text in the same centered spot when `data.label` is empty instead of a blank space; editing swaps in a new `LabelEditor` - a `nodrag nopan` `<textarea>` (those two class names are the standard xyflow convention, checked by its own pointer-down handling, for opting an element out of node-drag/pane-pan; also `stopPropagation`s its own `onPointerDown` as belt-and-suspenders) that's `autoFocus`ed with the caret moved to the end of the existing text on focus, calls the new `useReactFlow<CanvasNode, CanvasEdge>().updateNodeData(id, {label: value})` on every keystroke (same store-level `triggerNodeChanges` → `onNodesChange` → Liveblocks path as resizing), and closes on `onBlur` or `Escape` (which just calls `.blur()`, reusing the same blur-close path rather than a separate revert/cancel flow - not specced). Verified beyond the "Check When Done" list: a temporary public `/dev-preview` route (removed after, with its one-line `proxy.ts` addition) rendered two fixture nodes through a real controlled `useNodesState`/`onNodesChange` pair (a plain uncontrolled `nodes` prop with no change handler - tried first - silently no-ops on both resize and label edits, since nothing re-renders from a static array; this only reproduces the wiring `useLiveblocksFlow` already provides in the real app) and was driven with Playwright: double-clicking a diamond node's center opened a live-typable textarea whose value read back correctly and rendered into the label span on `Escape`; dragging a selected rectangle's bottom-right resize handle grew its measured box, and dragging it far past its origin clamped the box at exactly the 60×40 minimum (scaled by the current zoom level) rather than continuing to shrink. `tsc --noEmit` (via `next build`'s TypeScript pass, which includes Next's generated ambient types - a bare `tsc --noEmit` at the repo root fails on two pre-existing `PageProps`/`LayoutProps` references unrelated to this change), `eslint .`, `next build` all clean.
- 15-node-color-toolbar.md: step 1 (check `ui-context.md` for the palette) confirmed the doc already names `types/canvas.ts`'s existing `NODE_COLORS` (8 fill/text pairs, added back in 11-base-canvas.md) as the canonical palette, so no new palette was added - reused verbatim, no `globals.css` theme colors matched (these are canvas-specific, not app-chrome tokens). `components/editor/canvas-node.tsx` gained a new `ColorToolbar` component built on `<NodeToolbar nodeId={id}>` (from `@xyflow/react`) - `NodeToolbar` already only renders for a selected node and auto-positions itself above it without overlapping (default `position={Position.Top}`, `offset={10}`), so no custom visibility/positioning logic was needed to satisfy "only show when selected" / "slightly above, not overlapping". Renders one `h-5 w-5` round swatch button per `NODE_COLORS` pair: the active swatch (matching the node's current `data.color`) gets a persistent `border-2 border-copy-primary` ring, inactive swatches a faint `border-white/10`; hover adds a tight `box-shadow` glow via `hover:shadow-[0_0_6px_0px_var(--glow-color)]` with `--glow-color` set inline per-swatch to that pair's *text* color (not its fill), keeping the glow deliberately small/controlled rather than a blurred halo. Clicking a swatch calls `updateNodeData(id, { color: color.fill })` - the same `useReactFlow().updateNodeData` path 14-node-editing.md wired for labels, so it's already local-only and inside the existing Liveblocks-synced `onNodesChange` flow, no server calls; the node's text color needed no separate update since `CanvasNodeRenderer` already derives `textColor` by looking `data.color` back up in `NODE_COLORS` (from 12-shape-panel.md's original renderer), so background and text update together from one write. The toolbar's own container and every swatch button carry `nodrag nopan` so opening/using it never drags the node or pans the canvas. Verified beyond the "Check When Done" list: the same temporary `/dev-preview` route + Playwright pattern (removed after, with its one-line `proxy.ts` addition) - screenshotted the toolbar floating cleanly above a selected node with all 8 swatches, hovering a swatch showing its glow, and clicking one immediately repainting both the node's fill and its label's text color together, with the clicked swatch gaining the active ring. `tsc --noEmit` (via `next build`), `eslint .`, `next build` all clean.
- 16-edge-behavior.md: `types/canvas.ts`'s `CanvasEdge` gained real edge data (`CanvasEdgeData { label: string }`, replacing the placeholder `Edge<Record<string, never>, "canvasEdge">` from 11-base-canvas.md). `lib/canvas.ts` gained `createEdgeId(source, target)`, the same timestamp+counter scheme `createNodeId` already used. Step 1 (connection handles): `components/editor/canvas-node.tsx`'s 4 `Handle`s are now all `type="source"` (previously alternating source/target by position) - `@xyflow/system`'s `getEdgePosition` always resolves an edge's *source* endpoint from a node's `source`-typed handle bounds only, even under `ConnectionMode.Loose` (Loose only relaxes the *target*-side lookup to accept either type) - so a `type="target"` handle can never start a connection under any mode, which meant top/bottom (both `target`) were previously unusable as drag-start points; this was caught by an actual Playwright drag-and-drop test that logged React Flow's own error #008 ("Couldn't create edge for source handle") when connecting top-to-top, not discovered by reading docs alone. All 4 are also styled as small white dots with a dark border (`CONNECTION_HANDLE_STYLE`) and hidden by default, fading in via a new `group`/`group-hover:opacity-100` pairing - the outer shape wrapper `<div>` in all three shape branches gained `group`. Fixing this also surfaced a second real bug: the label-editing overlay div (`inset-0`, pointer-events enabled for double-click, added in 14-node-editing.md) sat flush on top of the handles at the node's border and swallowed their mousedown before a drag could start - changed to `inset-2` so a small margin around the node's edge stays uncovered and clickable, without shrinking the double-click catch area enough to matter visually. Step 2/3: new `components/editor/canvas-edge.tsx` - `CanvasEdgeRenderer`, registered as `edgeTypes.canvasEdge` in `canvas.tsx`, routes via `getSmoothStepPath` (right-angle routing with rounded corners) and renders through `BaseEdge` with a dimmed rest stroke (`rgba(225,225,235,0.45)`) that brightens (`rgba(240,240,245,0.95)`) on hover or `selected`, `strokeLinecap: "round"`, and `interactionWidth={24}` for a wider click target without thickening the visible line; a second, fully invisible wider `<path>` (BaseEdge's own built-in interaction path has no exposed event handlers) carries `onMouseEnter`/`onMouseLeave` for the hover-brighten state and `onDoubleClick` to open label editing. `components/editor/canvas.tsx`: `useLiveblocksFlow`'s own `onConnect` builds a plain untyped edge via `addEdge` internally with no way to make it a `canvasEdge`, so - mirroring how `addNode` already bypasses the hook's default node-add behavior - a new `handleConnect` constructs the `CanvasEdge` itself (`type: "canvasEdge"`, `markerEnd: {type: MarkerType.ArrowClosed}` for the arrowhead, empty label) and adds it via `onEdgesChange([{type: "add", item: newEdge}])`; `connectionLineType={ConnectionLineType.SmoothStep}` makes the in-progress drag preview match the eventual routing. Step 4 (label editing): `EdgeLabelRenderer` positions a `<div>` at the `labelX`/`labelY` `getSmoothStepPath` itself returns (no manual midpoint math, per spec); double-clicking the edge or its label opens a new `EdgeLabelEditor` - a `size={Math.max(draft.length, 1)}` `<input>` (the "grows with the text" trick) holding local `draft` state, saved via `updateEdgeData(id, {label: value})` only on blur/Enter/Escape (Enter and Escape both just call `.blur()`, reusing one commit path) rather than live-per-keystroke like node labels in 14-node-editing.md, since this spec's own wording ("save on blur, Enter, or Escape") reads as an explicit, different choice from 14's "update as users type." Saved labels render as a `rounded-full` pill badge; an unlabeled edge shows a faint dashed-border "Double-click to add a label" hint only while hovered/selected, not at rest. The label `<div>` and the `EdgeLabelEditor` input both carry `nodrag nopan`, and the input also `stopPropagation`s its own `onPointerDown`, so label interactions never drag a node or pan the canvas. Verified beyond the "Check When Done" list: the same temporary `/dev-preview` route + Playwright pattern (removed after, with its one-line `proxy.ts` addition) - confirmed handle opacity is exactly `0`/`1` hidden/hovered; confirmed the edge's computed `stroke` literally changes between the dim and bright rgba values on label-hover and on click-to-select; confirmed double-clicking a label opens a real, typable `<input>` whose value reads back into the edge's saved pill label after `Enter`; and, after the handle-type fix, confirmed all three previously-impossible-or-untested pairings - top-to-top, bottom-to-bottom, and right-to-left - each successfully create a rendered `canvasEdge` with no console errors, where the top-to-top pairing specifically reproduced and then verified the fix for the handle-type bug above. `tsc --noEmit` (via `next build`), `eslint .`, `next build` all clean.
- 16-nodes-color-toolbar.md: byte-for-byte identical to 15-node-color-toolbar.md (`diff` confirmed, no differences) - implemented already, see that entry above. No new code changes; re-verified `ColorToolbar` (component, its wiring into all three `CanvasNodeRenderer` shape branches, and its `updateNodeData` swatch handler) was untouched by 16-edge-behavior.md's edits to the same file (`components/editor/canvas-node.tsx`) and still builds clean.
- 17-canvas-ergonomics.md.md: new `components/editor/canvas-controls.tsx` - `CanvasControls`, a floating pill (`absolute bottom-4 left-4 z-20`, matching the `ShapePanel`/`ColorToolbar` floating-overlay token pattern) with two button groups separated by a `w-px` divider: zoom out/fit view/zoom in (from `useReactFlow()`'s `zoomIn`/`zoomOut`/`fitView`, each called with `{duration: 250}` for the "short animation" the spec asked for) and undo/redo, passed in as props (`canUndo`/`canRedo`/`onUndo`/`onRedo`) so the component stays a dumb view and both the control bar's buttons and `useKeyboardShortcuts` share the exact same handlers rather than each wiring Liveblocks separately. `components/editor/canvas.tsx` sources those handlers from `@liveblocks/react`'s `useUndo`/`useRedo`/`useCanUndo`/`useCanRedo` (existing hooks, exactly as the spec named) and passes `canUndo`/`canRedo` straight through to `disabled` on the undo/redo buttons, which get `disabled:opacity-30` for the "keep disabled buttons visually dimmed" requirement. New `hooks/use-keyboard-shortcuts.ts` - `useKeyboardShortcuts({reactFlowInstance, onUndo, onRedo})` (generic over the node/edge types, so it isn't hard-coded to `CanvasNode`/`CanvasEdge`), a single `window` `keydown` listener bailing out immediately via a new `isEditableTarget()` check (`INPUT`/`TEXTAREA`/`isContentEditable`) before matching any shortcut - `+`/`=` → `zoomIn`, `-` → `zoomOut`, `Cmd/Ctrl+Z` → undo (or redo with Shift held), `Cmd/Ctrl+Y` → redo, all `event.preventDefault()`-ed so they don't also trigger the browser's own native zoom/undo. Item 6 (minimap minimize/reopen): new `components/editor/canvas-minimap.tsx` - `CanvasMinimap`, rendered inside `<ReactFlow>` in place of the old bare `<MiniMap />`, wraps both states (open: the map + a small `Minus`-icon circle at its corner; minimized: a single `Map`-icon circle) in one `<Panel position="bottom-right">` so the toggle stays anchored to the same corner instead of jumping - required overriding `MiniMap`'s own `style` to `position: "static"` since `MiniMap` always wraps itself in *its own* internal `Panel`, and nesting two independently-`position: absolute`-ed panels at the same corner would otherwise stack them directly on top of each other rather than laying the map out normally inside mine. Verified beyond the "Check When Done" list: a temporary public `/dev-preview` route (removed after, with its one-line `proxy.ts` addition) mocked local undo/redo state (a real Liveblocks room needs a signed-in session and a connected server round-trip, same gap noted as accepted in every canvas spec's verification since 11-base-canvas.md) and was driven with Playwright - confirmed the undo/redo buttons' `disabled` state flips correctly after a click, `Cmd/Ctrl+Shift+Z` triggers redo and re-enables undo, zoom in/out/fit-view all move the `.react-flow__viewport` transform's `scale`/`translate` (zoom-in capping at React Flow's default `maxZoom` of 2, confirmed by first zooming out then back in), the `-` key zooms out identically to the button, and the minimap correctly disappears to a small icon on minimize and fully reappears on reopen. One click on the zoom-out button was blocked mid-test not by a real bug but by `next dev`'s own bottom-left dev-tools indicator (`<nextjs-portal>`) physically covering that exact corner in local dev only (confirmed via `elementFromPoint`, and reproduced only right after an HMR recompile) - not present in production builds, so left as-is rather than moving the bar off its spec'd "bottom-left" position; the underlying handler was separately confirmed working via a direct DOM `.click()` bypassing the overlay. `tsc --noEmit` (via `next build`), `eslint .`, `next build` all clean.
- 18-starter-templates.md: new `components/editor/starter-templates.ts` - `CanvasTemplate` (`id`/`name`/`description`/`nodes`/`edges`, using the real `CanvasNode`/`CanvasEdge` types) and `CANVAS_TEMPLATES`, four templates (Microservices Architecture, CI/CD Pipeline, Event-Driven System, Kubernetes & Docker Architecture - the spec's own four examples) built with two small readability helpers, `node()` and `edge()`, that fill in each shape's real `NODE_DEFAULT_SIZES` and reuse `NODE_COLORS` entries (destructured once into named consts like `BLUE`/`TEAL`) and the same arrowhead marker `canvas.tsx` gives live-drawn edges, so template data stays just id/label/shape/color/position/connections rather than repeating boilerplate. New `components/editor/starter-templates-modal.tsx` - `StarterTemplatesModal`, a `Dialog` with a `ScrollArea`-wrapped `sm:grid-cols-2` grid of cards (name, description, an `Import` button calling `onImport(template)` then closing itself). Step 3's preview lives in the same file as `TemplatePreview` - no React Flow instance, just a `<svg>` whose `viewBox` is computed from the union of every template node's position+size (`computeBounds`) with fixed padding, `preserveAspectRatio="xMidYMid meet"` fitting that into a fixed `h-32` viewport; edges are drawn as plain `<line>`s between node centers, and nodes are drawn by a new `PreviewNode` that reuses the exact same exported geometry constants `CanvasNodeRenderer` itself draws from (`CSS_SHAPE_RADIUS`, `POLYGON_POINTS`, `CYLINDER_BODY_PATH`/`CYLINDER_RIM_PATH`, from `components/editor/canvas-node.tsx`) so a card's preview always matches what actually gets imported, not a separate approximation. `lib/canvas.ts` gained `instantiateTemplate(template)` - remaps every template node to a fresh id via the existing `createNodeId`, and every edge's `source`/`target` (plus a fresh id via `createEdgeId`) through that remapping, so importing the same template twice (or two collaborators importing at once) never collides on the template's own static ids. Step 4 wiring: `components/editor/canvas.tsx` gained `isTemplatesModalOpen`/`onTemplatesModalOpenChange` props, renders `<StarterTemplatesModal>`, and its `handleImportTemplate` clears the canvas first - `onNodesChange`/`onEdgesChange` each get a single batched array of `remove` changes for every current node/edge followed by `add` changes for the instantiated template, ensuring the replace happens as one atomic set of changes rather than a clear-then-add race - then sets a `pendingFitViewRef` flag that a `useEffect` watching `nodes` consumes to call `fitView({duration: 300})` once the post-import nodes have actually flowed through, rather than fitting against the stale pre-import viewport. `components/editor/canvas-room.tsx` threads the same two props through to `Canvas` (needed since the trigger button lives in the navbar, outside the `CanvasRoom`/Liveblocks-room subtree entirely). `components/editor/editor-shell.tsx` owns the `isTemplatesOpen` state and adds a `LayoutTemplate`-icon navbar button (in the same `actions` slot as Share/AI-sidebar, only rendered when `activeProject` exists since there's no canvas otherwise) that opens the modal, passing the state down to `CanvasRoom`. Verified beyond the "Check When Done" list: a temporary public `/dev-preview` route (removed after, with its one-line `proxy.ts` addition) rendered the modal over a plain `ReactFlow` instance seeded with one pre-existing node and was driven with Playwright - confirmed all 4 cards render with visually-correct shape/color previews matching their template data, clicking Import replaces the pre-existing node entirely (node count went from 1 to the template's own 7, the old node's text no longer found in the DOM) rather than adding on top, the modal closes itself after import, and the final canvas is both fully connected and fit to view. This surfaced one real gap in the dev-preview harness itself (not the product code): the harness's plain `<ReactFlow>` was missing `connectionMode={ConnectionMode.Loose}`, which `components/editor/canvas.tsx` already sets - without it, `@xyflow/system`'s `getEdgePosition` (the same "source must be a `source`-typed handle" behavior root-caused in 16-edge-behavior.md) couldn't resolve any of the template's edges and silently dropped all of them; adding the missing prop to the harness (matching the real component) made all 6 imported edges render correctly, confirming the actual `canvas.tsx`/template-edge data was correct all along. `tsc --noEmit` (via `next build`), `eslint .`, `next build` all clean.
- 19-presence-avatars-cursors.md: `liveblocks.config.ts`'s `Presence.isThinking` renamed to `thinking` per the spec's exact field name (also updated in `components/editor/canvas-room.tsx`'s `initialPresence`, the only other reference). Step 1 required no navbar changes at all - `components/editor/editor-navbar.tsx` was left completely untouched, and the new presence UI is mounted only inside `Canvas` (which itself only renders when `activeProject` exists), so it's structurally impossible for it to appear on the editor home navbar without a separate, unused code path. New `components/editor/presence-avatars.tsx` - `PresenceAvatars`, an `absolute top-4 right-4 z-20` floating pill (same token pattern as every other canvas overlay) that calls `useUser()` (Clerk) and `useOthers()` (Liveblocks) directly; filters `others` by `other.id !== user?.id` rather than trusting `useOthers()`'s own self-exclusion, since that only excludes the current *connection* - the same Clerk user with the project open in two tabs would otherwise show up as a second "collaborator" in each other's list. Shows up to 5 overlapping `CollaboratorAvatar`s (`ring-2 ring-surface`, Clerk `avatar` URL or an initials fallback styled like `share-dialog.tsx`'s existing avatar treatment) plus a `+N` overflow chip past 5, a divider only rendered when `collaborators.length > 0`, and the existing Clerk `<UserButton>` for the current user last - sized to match via `appearance.elements.userButtonAvatarBox` since Clerk's default avatar size wasn't guaranteed to equal the custom avatars' `h-8 w-8`. New `components/editor/canvas-cursors.tsx` - `CanvasCursors`, rendered as a child of `<ReactFlow>` inside `@xyflow/react`'s `ViewportPortal` (keeps cursor markers in the same coordinate space as nodes/edges, so they pan with the canvas and hold their screen size through zoom) - one `MousePointer2` (lucide) + name badge per other participant with a non-null `presence.cursor`, both colored from that participant's `info.color` (the deterministic per-user color `lib/liveblocks.ts` already assigns during auth). `components/editor/canvas.tsx` gained `onPaneMouseMove`/`onPaneMouseLeave` on `<ReactFlow>` (the actual prop names for "React Flow's mouse move/leave event" - there's no bare top-level `onMouseMove`) wired to a new `useMyPresence()` call: mouse-move converts the event's screen position to flow-space via the same `screenToFlowPosition` `handleDrop`/`handleAddShape` already use and broadcasts `{cursor: position}`; mouse-leave broadcasts `{cursor: null}`. Verified beyond the "Check When Done" list: rather than mocking presence, a temporary pair of routes (`/api/dev-preview-auth` issuing real Liveblocks sessions for two fake identities, `/dev-preview` reading `?userId&name&color` to join a shared room - both removed after, with `proxy.ts`'s one-line addition) was driven with **two separate Playwright browser contexts** connected to the same real Liveblocks room simultaneously - confirmed each side sees exactly one collaborator avatar (the other user, never itself), moving the mouse in one context renders a correctly-positioned, correctly-colored, correctly-named cursor+badge in the *other* context's view only (never its own), and moving the mouse off the pane clears that cursor on the other side within one presence update - a full real multi-client round trip, not a single-client approximation. `tsc --noEmit` (via `next build`), `eslint .`, `next build` all clean.
- 20-ai-sidebar-shell.md: `components/editor/ai-sidebar.tsx` rewritten in place - the file was already its own component (from 08-editor-workspace-shell.md's placeholder), so step 1 meant building out its internals rather than extracting a new file; `isOpen`/`onClose` stayed parent-controlled exactly as before (`editor-shell.tsx` untouched), and the existing floating/slide styling (`absolute top-0 right-0`, `translate-x-full` ↔ `translate-x-0`, `duration-200 ease-in-out`) was kept as-is - only the background token changed from the placeholder's `bg-surface/95` to the spec's explicit `bg-base/95`, and a `shadow-lg` was added (matching every other floating overlay's shadow in this codebase - `ShapePanel`/`CanvasControls`/`ColorToolbar` - since the placeholder had none to "preserve" despite the spec's wording). Step 6 required reconciling the spec's own token names against the real ones in `app/globals.css`/`ui-context.md`, none of which exist verbatim: `text-primary-text`/`text-muted-text` → `text-copy-primary`/`text-copy-muted`, `bg-brand-dim` → `bg-accent-dim` (the actual "dim" token, same `--accent-primary` family `border-brand` already draws from), and - the one genuine judgment call - `bg-accent`/`text-accent` mapped to the `ai`/`ai-text` token pair (`--accent-ai`/`--accent-ai-text`, a purple distinct from the teal `brand` used for the rest of the app's chrome) rather than `brand`, since the existing placeholder already used `text-ai-text` for its icon and the spec itself names `bg-brand-dim`/`border-brand` *separately* from `bg-accent`/`text-accent` for user-message styling - confirming the spec deliberately treats "accent" (AI-specific purple) and "brand" (general teal) as two different things. Active tabs render `bg-ai/15 text-ai` rather than a literal opaque `bg-ai` - solid accent-on-accent for both fill and text would be illegible, and a soft-tint-plus-colored-text pairing is the same pattern the spec itself uses for starter chips (`bg-subtle`/`text-accent-text`), so this reads as the intended visual rather than a deviation. Chat UI: a local `ChatMessage[]` array (`useState`, no persistence) holds only user-authored messages - no fake/demo assistant reply is ever generated, since that would cross into the explicitly out-of-scope "AI generation logic"; the `ChatBubble` component still defines both `user` (`border-2 border-brand/50 bg-accent-dim`) and `assistant` (`bg-elevated border border-surface-border text-ai-text`) styles so the assistant treatment exists and is ready once a real reply source exists. Starter chips fill the draft textarea rather than sending immediately (`onPromptClick={setDraft}`), letting the user review/edit before submitting - a judgment call, since the spec doesn't specify chip-click behavior. `Textarea` (shadcn) already auto-resizes via its own `field-sizing-content`; only `min-h-[72px] max-h-[160px]` needed overriding to match the spec's exact band. `Enter` submits (`preventDefault` + send) and `Shift+Enter` inserts a newline via the textarea's own default behavior (no `preventDefault` on that branch). Specs tab: a static `DemoSpecCard` (`FileText` icon, title, snippet, `Download` button rendered with the shadcn `Button`'s own `disabled` prop) sits below a `Generate Spec` button - neither wired to anything, per scope limits. Verified beyond the "Check When Done" list: a temporary public `/dev-preview` route (removed after, with its one-line `proxy.ts` addition) rendered `AiSidebar` standalone and was driven with Playwright - confirmed clicking a starter chip fills the textarea with its exact text, pressing `Enter` sends it and renders a right-aligned user bubble with the correct content, `Shift+Enter` inserts a real newline into the textarea value instead of submitting, switching to the Specs tab shows the demo card with a `disabled` Download button, and the close button slides the sidebar fully off-screen. `tsc --noEmit` (via `next build`), `eslint .`, `next build` all clean.
- 29-spec-ui-integration.md: implemented last, as instructed, after 27/28 landed. Rewrote `SpecsTab` in `components/editor/ai-sidebar.tsx` entirely - the old static `DemoSpecCard` placeholder from 20-ai-sidebar-shell.md is gone. New `SpecListItem` fetches `GET /api/projects/{roomId}/specs` on mount (same fetch-on-mount + `eslint-disable react-hooks/set-state-in-effect` pattern `hooks/use-share-dialog.ts` already established) and renders each spec's derived `filename` + `createdAt` (formatted via `toLocaleString`) in a compact scrollable list, clickable to open a preview. New `SpecPreviewDialog` (shadcn `Dialog`) fetches the spec's Markdown through the *same* download route (`fetch()` just reads the response body and ignores its `Content-Disposition` header - the spec's own "do not access Blob directly from the client" plus "assume content must be fetched separately" together rule out a third endpoint, and the download route already returns exactly the right bytes) and renders it with `react-markdown` (newly installed - no markdown renderer existed in this codebase; `marked` was only present as an undeclared transitive dependency of `@liveblocks/node`, too fragile to depend on directly) through a small custom `components` map styled with this app's own existing tokens (`text-copy-primary`/`text-copy-secondary`/etc.) rather than pulling in `@tailwindcss/typography`, which also isn't installed - "use existing colors and tokens" read as a reason not to add a whole styling plugin for one modal. Content is held in local `useState` only while the modal is open, cleared on close (not cached) - satisfies "do not store spec content in frontend state long-term" literally. Download (both the list row's icon button and the modal's footer button) is `window.open(downloadUrl, "_blank")`, not a fetch+blob dance - a real browser navigation is what actually honors the download route's `Content-Disposition: attachment` header; `fetch()` alone (as the preview path uses) would not trigger a save dialog. `DialogClose` uses this codebase's base-ui `render` prop convention (`share-dialog.tsx`'s exact pattern), not a nonexistent `asChild`. The "Generate Spec" button is untouched and still does nothing - assembling `chatHistory`/`nodes`/`edges` and calling `POST /api/ai/spec` isn't one of this spec's 3 listed implementation steps (list/preview/download), so it wasn't guessed at; recorded as a real, known gap rather than silently left broken. Verified: `next build` (`tsc`), `eslint .` both clean (one real lint catch along the way: the preview dialog's close-reset branch synchronously called `setState` inside an effect with no fetch to justify it, same rule `use-share-dialog.ts` already works around - fixed with the same documented exception, not suppressed blindly).
- 28-spec-persistence-download.md: new `prisma/models/project-spec.prisma` - `model ProjectSpec` (`id`/`projectId`/`filePath`/`createdAt`, exactly the 4 fields the spec listed, metadata-only per its own "do not store spec content in Prisma") plus a matching `specs ProjectSpec[]` back-relation added to `Project` in `prisma/models/project.prisma` (Prisma requires the opposite relation field to exist); `prisma migrate dev --name add_project_spec` applied cleanly. Persistence itself lives inside `trigger/generate-spec.ts` (not a separate API route - the spec's own "after a spec is generated" reads as "as part of the background task completing," and step 2 lists no new save endpoint, only the step-3 download route) - after `generateText` returns, a `randomUUID()`-generated `specId` doubles as both the Blob pathname's unique component and the row's explicit `id` (matching `context/architecture-context.md`'s exact documented path, `specs/{projectId}/{specId}.md`) so there's no window where a row exists pointing at a blob that hasn't been written yet, or vice versa; `put()` uses the same `access: "private"` treatment `app/api/projects/[projectId]/canvas/route.ts` already established for this Blob store (confirmed private-only in 21-canvas-autosave.md), just `addRandomSuffix: false` with no `allowOverwrite` (unlike canvas snapshots, each spec is a new, never-overwritten file, so a collision would be a genuine bug worth failing loudly on rather than silently allowing). New `GET /api/projects/[projectId]/specs/[specId]/download/route.ts` - `checkProjectAccess`-gated (401/403, same pattern as every other project-scoped route), looks up the `ProjectSpec` and checks `spec.projectId !== projectId` before trusting it (same not-found-vs-wrong-project non-disclosure shape `checkProjectAccess` itself already uses, so a spec id valid for a *different* project 404s instead of leaking that it exists), then streams the private blob via `get(spec.filePath, {access: "private"})` (identical to the canvas route's read side) with `Content-Disposition: attachment`. Also added `GET /api/projects/[projectId]/specs/route.ts` (a list endpoint) even though it's not one of spec 28's 3 numbered steps - 29-spec-ui-integration.md's own step 1 says "fetch specs from the backend using the **existing** ProjectSpec API," which would be impossible to honor without something to fetch from; the list response deliberately omits `filePath` entirely (only `id`/`createdAt`/a derived `filename`), keeping the Blob URL itself reachable only from inside the download route, never handed to the client directly - satisfies 28's "do not expose Blob URLs without access checks" and 29's "do not access Blob directly from the client" for the list view specifically. Verified: `next build` (`tsc`), `eslint .`, `trigger dev` (confirms `generate-spec` still bundles with the new Blob/Prisma imports) all clean.
- 27-spec-generation-flow.md: checked `context/project-overview.md`/`context/architecture-context.md` first per the spec's own Notes, confirming spec generation should follow the same durable-background-task shape design generation already uses. `types/tasks.ts` gained `specNodeSchema`/`specEdgeSchema` (a minimal, permissive Zod projection of the canvas graph - just `id`/`position`/`data.label`/`data.shape`/`data.color` for nodes, `id`/`source`/`target`/`data.label` for edges; deliberately not the full React Flow `Node`/`Edge` shape, most of which is rendering-only and irrelevant to a written spec, and explicitly not a change to `types/canvas.ts`'s own `CanvasNode`/`CanvasEdge` - per "do not change existing canvas or chat data models," this is a read-only projection, not a schema swap), `generateSpecRequestSchema` (`roomId`/`chatHistory`/`nodes`/`edges`, no `projectId` field at all - see below), and `generateSpecPayloadSchema` (the request schema `.extend()`-ed with a server-resolved `projectId`). New `app/api/ai/spec/route.ts` mirrors `app/api/ai/design/route.ts`'s exact shape (401/403 gating via `getCurrentIdentity`/`checkProjectAccess`, `tasks.trigger` with a type-only task import, a `TaskRun` row for later ownership checks) with one deliberate difference the spec calls out explicitly: "do not trust a client-supplied projectId" - so the request body has no `projectId` field to trust in the first place, and the task payload's `projectId` comes from `access.id` (the value `checkProjectAccess` itself already resolved and verified server-side), not anything read off the request. New `app/api/ai/spec/token/route.ts` mirrors `app/api/ai/design/token/route.ts` identically except for the spec's one explicit difference - `expirationTime: "1h"` on `auth.createPublicToken` (design's token route left this at Trigger.dev's own 15-minute default, since 23-design-agent-logic.md never specified one). New `trigger/generate-spec.ts` - `generateSpecTask` uses `schemaTask` (not plain `task()` with manual `.parse()`) specifically because this spec's own wording, "validates input with Zod," is exactly what `schemaTask` exists for per the `trigger-authoring-tasks` skill (a bad payload throws `TaskPayloadParsedError` and skips retrying automatically, no manual validation branch needed) - a genuine, deliberate difference from `design-agent.ts`'s plain `task()`, which validates an *LLM's output* with Zod, not its own *input payload*, a different problem `schemaTask` doesn't address. `buildPrompt` summarizes nodes (label + shape) and edges (source label → target label, resolved through a node-id map) plus the most recent 30 chat messages into one prompt, and calls `generateText` with **no** `output`/schema config at all - unlike `design-agent.ts`'s structured `Output.object` plan, a Markdown document has no schema to satisfy or fail against, so this sidesteps that entire earlier Gemini-structured-output problem class by construction rather than by careful schema design. `metadata.set("status", ...)` calls (`reading-context`/`generating`/`complete`, later `saving` added in 28's follow-up work below) track progress via Trigger.dev's own run metadata - deliberately not `ai-status-feed`/Liveblocks, since spec 27 never mentions Liveblocks at all for spec generation and metadata is what a future `useRealtimeRun` caller would read directly off the run itself, per "updates run metadata/status for realtime tracking." Reused the existing default `google` provider from `@ai-sdk/google` (already configured via `GOOGLE_GENERATIVE_AI_API_KEY`) with no new provider wrapper, per "do not create a new AI provider abstraction." Verified: `next build` (`tsc`), `eslint .`, `trigger dev` (confirms `generate-spec` bundles alongside `hello-world`/`design-agent` with no registration errors) all clean.
- 26-ai-chat-functional.md: step 1's `{runId, publicToken}` from one `POST /api/ai/design` response reads as a single call, but the scope limit "Do not implement backend or Trigger.dev logic" rules out merging 22-design-agent-api.md's deliberately separate `/api/ai/design` (trigger + record ownership) and `/api/ai/design/token` (mint a run-scoped token, ownership-checked) routes into one - so the sidebar chains both existing calls client-side instead, landing on the same effective `{runId, publicToken}` state without touching either route. That chaining did surface one real bug against the *existing* route, not new backend work: `/api/ai/design` requires `projectId` in its body (for `checkProjectAccess`) in addition to `roomId` - spec 26's abbreviated `{prompt, roomId}` wording would have 400'd against the real endpoint, so the sidebar sends both, `projectId: room.id`, reusing the established "the project id is the room id" convention rather than inventing a lookup. New `useRealtimeRun(activeRun?.runId, {accessToken: activeRun?.publicToken, enabled: activeRun !== null})` (`@trigger.dev/react-hooks`, already a direct dependency) - confirmed via its `useApiClient` source that passing `enabled: false` alongside an undefined `accessToken` returns `undefined` rather than throwing, so the hook is safe to call unconditionally rather than needing to be mounted/unmounted around the run's lifetime. A new `activeRun` local state (`{runId, publicToken}`, submitter-only - not shared with other participants) drives it; a `useEffect` keyed on `run?.isCompleted` (not the whole `run` object, so it fires once per terminal state rather than on every intermediate status tick) pushes a final assistant message to `ai-chat` (`run.isSuccess` picks between a success/failure message) and resets `activeRun`/`isSubmitting`, per step 2. Chat-input disabling now OR's two signals: the shared `ai-status-feed`-derived `aiStatus.isActive` (still visible to every participant, unchanged from 24/25) and a local `isSubmitting` flag covering the brief window between clicking Send and the task's own first status publish landing in the feed - so a second participant's input disables promptly even before `ai-status-feed` catches up, and one room can't kick off two overlapping generations. Step 3 ("do not manually update nodes/edges... rely on Liveblocks to reflect changes") required no new code at all - `trigger/design-agent.ts` already writes straight into the room's `flow` Storage tree (23-design-agent-logic.md) and `components/editor/canvas.tsx`'s `useLiveblocksFlow` already renders whatever's in that tree, so AI-driven canvas updates were already automatic; this step was satisfied by not touching either file. Step 4's status strip **moved** rather than duplicated: 24-ai-presence-state.md's header-level `AiStatusIndicator` is gone, replaced by a new `AiStatusStrip` living inside `AiArchitectTab` directly above the form - spec 26's own "UI Details" section gives an explicit position ("above the input") and look ("dark base + green accent... subtle animated indicator") that 24 never specified, so this reads as refinement of the same concept rather than scope creep or a redesign. UI Details' exact literal hex (`#62C073`) turned out to already be a real value in this codebase - `NODE_COLORS`'s green pair in `types/canvas.ts`/`context/ui-context.md` (`{fill: "#0F2E18", text: "#62C073"}`), not `--state-success` (`#34d399`, a different green used for app-chrome) - so "do not introduce new colors" was read as "use this value, which already exists," applied via inline `style` (Tailwind's arbitrary-value classes don't support runtime-computed opacity suffixes cleanly for a value pulled from a shared constant) rather than a new CSS custom property, since the spec gave literal hexes rather than asking for new tokens to be defined. User chat bubbles switched from the old bordered/tinted treatment to a solid `#62C073` background with `#0F2E18` text (the same pair, inverted - dark-on-light instead of light-on-dark, for contrast) per "green accent background, readable contrast text"; AI bubbles and the disabled/dimmed button state were left on their existing `bg-ai`/`text-ai-text`/`disabled:opacity-50` tokens since the spec's "dark background, light text" and "dimmed state" wording didn't call for anything different from what already existed. `pushChatMessage`'s catch path (both the design-trigger and token-mint failures) posts an assistant-role error message to `ai-chat` per the "Show errors as messages in ai-chat feed" General bullet, distinct from 25's existing inline `sendFailed` text (which stays scoped to "failed to send the chat message itself," a different failure mode). Verified: `next build` (`tsc`), `eslint .`, and `trigger dev` (unaffected - this spec touched no `trigger/` file) all clean.
- 25-sidebar-chat-feed.md: step 1's "check the existing Liveblocks setup and follow the same feed patterns already used" pointed straight at 24-ai-presence-state.md's `ai-status-feed` work - `ai-chat` reuses the exact same shape (a room-scoped Liveblocks Feed, `useFeedMessages` to read, validated Zod payloads in `types/tasks.ts`) with a genuinely new mechanism only where it has to differ: **client-side** creation and sending, since chat is something room participants themselves produce, not a backend task. New `useEnsureFeed(feedId)` in `components/editor/ai-sidebar.tsx` - the client-hook equivalent of `trigger/design-agent.ts`'s server-side `ensureAiStatusFeed`, calling the client `useCreateFeed()` once on mount and swallowing any error (an already-existing feed is the expected steady state); called for `ai-chat` only - `ai-status-feed` still gets created server-side by `design-agent.ts` the first time it actually runs, so the sidebar doesn't need its own creation path for that one. `types/tasks.ts` gained `chatFeedMessageSchema` (`sender`/`role`/`content`/`timestamp`, per the spec's step 4 - `FeedMessage` itself carries no author info, so sender has to be part of the payload, not read off Liveblocks' own message metadata) and `AI_CHAT_FEED_ID = "ai-chat"`, kept as a fully separate constant/schema from `AI_STATUS_FEED_ID`/`aiStatusFeedMessageSchema` per the explicit "don't mix chat messages with status messages" scope limit - the two feeds share no code path beyond both being Feeds. New `useChatMessages()` hook wraps `useFeedMessages(AI_CHAT_FEED_ID)`, validates each message through `chatFeedMessageSchema.safeParse` (an invalid message is dropped, not rendered - same non-crashing posture as `useAiStatus()` from 24), and sorts by the payload's own `timestamp` rather than trusting feed array order (undocumented) - satisfies "render chat messages in order" without assuming an implementation detail. `AiArchitectTab`'s send path now calls the client `useCreateFeedMessage()` hook directly (no new API route - Liveblocks' client SDK can write feed messages straight from an authenticated room connection, same trust model already established for presence/storage), building the sender name from Clerk's `useUser()` (`fullName` → primary email → "Anonymous", matching `presence-avatars.tsx`'s existing fallback chain) - clears the draft only after a successful send, and a caught failure sets a `sendFailed` flag rendering a small inline `text-error` message (existing token, no new color) below the textarea rather than a toast or blocking state. `ChatBubble` now takes the real `ChatFeedMessage` shape and renders a sender name + `toLocaleTimeString` timestamp line above each bubble (new, not in the prior local-only version) while keeping the same bubble/color treatment `20-ai-sidebar-shell.md` established, satisfying "keep the styling consistent with the existing sidebar UI." No AI-generated replies, no background task triggering, no `/api/ai/design` call anywhere in this pass - purely collaborative human-to-human sidebar chat, per the scope limits; that wiring is 26-ai-chat-functional.md, done next in this same session. Verified: `next build` (`tsc`), `eslint .` both clean.
- 24-ai-presence-state.md: "check the existing Liveblocks setup ... first" (step 2) surfaced a real conflict with 23-design-agent-logic.md's own work - that spec's `RoomEvent`-based `"ai-status"` broadcast was exactly the kind of "parallel realtime state" this spec's "follow Liveblocks best practices for feeds/presence" line says to avoid once a purpose-built primitive (a Liveblocks *Feed* - `createFeed`/`createFeedMessage`/`useFeedMessages`, a different, more recent Liveblocks product feature from `RoomEvent`/`broadcastEvent`) exists for "the latest message in a named stream." Migrated rather than layered: `trigger/design-agent.ts`'s `publishStatus` now calls `liveblocks.createFeedMessage({roomId, feedId: "ai-status-feed", data})` instead of `broadcastEvent`, with a new `ensureAiStatusFeed` ("create or reuse", per the spec's own wording) that calls `createFeed` and swallows a 409-already-exists; `liveblocks.config.ts`'s `RoomEvent` type reverted to its prior empty/unspecced placeholder since nothing publishes to it anymore. Confirmed via the Node SDK's own types that Feeds are room-scoped (`createFeed`/`createFeedMessage`/etc. all take an explicit `roomId`, and the client hooks' own doc comments say "for the current room") - so the literal feed id `"ai-status-feed"` from the spec is correct as-is, with no need to manually embed the room id into it. New `types/tasks.ts` (the file the spec named explicitly) - `AI_STATUS_VALUES`/`AiStatus` (reused from 23's now-removed `RoomEvent` shape), `ACTIVE_AI_STATUSES` (which statuses count as "generation in progress"), `aiStatusFeedMessageSchema` (Zod, `status` + optional `text` per the spec's step 3), and the `AI_STATUS_FEED_ID` constant both the task and the sidebar import rather than each hardcoding the string. Discovered `useFeedMessages`/`useCreateFeed` require being inside the room's own `RoomProvider` (feeds are room-scoped realtime state, not global) - but `AiSidebar` was rendered as a plain sibling of `CanvasRoom` in `editor-shell.tsx`, outside any Liveblocks context at all. Rather than open a second connection to the same room (a `avoid-hitting-user-limit-in-rooms`-flavored anti-pattern) or move `AiSidebar` inside `Canvas`'s own `ClientSideSuspense`/`ReactFlowProvider` subtree (which would gate it behind canvas-load and couple it to React Flow), `components/editor/canvas-room.tsx` gained a `children` prop rendered inside `RoomProvider` but as a sibling of `CanvasErrorBoundary` - `editor-shell.tsx` now passes `<AiSidebar>` as `CanvasRoom`'s children instead of a separate flex sibling; since `RoomProvider`/`LiveblocksProvider` emit no DOM wrapper, `AiSidebar`'s existing `absolute top-0 right-0` positioning still resolves against the same outer `relative` container as before, so this was a pure logical-nesting change with no visual/layout difference. `components/editor/ai-sidebar.tsx`: new `useAiStatus()` hook wraps `useFeedMessages(AI_STATUS_FEED_ID)`, picks the message with the highest `createdAt` (ordering isn't documented, so this doesn't assume the array is pre-sorted) rather than just `messages[0]`/`.at(-1)`, and validates it through `aiStatusFeedMessageSchema.safeParse` per the spec's step 3 - an unparsable or missing message just means no status to show, not a crash. A new `AiStatusIndicator` renders under the header only while `aiStatus.isActive` (started/thinking/generating, not complete/error) - satisfies "show a small status indicator when AI is working" literally, visible identically to every participant since it's reading the shared feed, not local state. `AiArchitectTab` gained an `isGenerating` prop: the `Textarea` and send `Button` both get `disabled={isGenerating}`, the send button's icon/label swap to a spinning `Loader2`/"Generating…", and `submitDraft` bails early if generation is active as a second guard beyond the disabled attribute - nothing else in the sidebar (tabs, Specs tab, close button) is touched, per the "don't block or dim the whole sidebar" scope limit. `components/editor/canvas-cursors.tsx` - each cursor's name badge now renders a small spinning `Loader2` before the name when `other.presence.thinking === true` (already-declared `Presence.thinking`, no new field), hidden otherwise; only shows for participants who also have a live cursor position, same as the rest of the badge. The AI sidebar's chat input still isn't wired to actually call `/api/ai/design` - out of scope per this spec's own "don't add actual AI generation logic" / "don't trigger background tasks yet" limits; typing and hitting Send still only appends a local, never-sent chat bubble. Verified: `next build` (`tsc`), `eslint .`, and `trigger dev` (confirms `design-agent` still bundles/registers after the Feed-based rewrite) all clean.
- 23-design-agent-logic.md: checked `context/project-overview.md` ("Output is structured as canvas nodes and edges written into the shared room") and `context/architecture-context.md` ("Output: structured node and edge updates written into the shared Liveblocks room") first, per the spec's own step 1 instruction, confirming the task must mutate Storage directly rather than only notify a connected client. Also loaded the `liveblocks-best-practices` skill's `ai-as-a-collaborator` reference, which points at Liveblocks' "Integrating AI Agents as Liveblocks Collaborators" guide (ephemeral presence + server-side storage mutation as the documented pattern for AI-as-collaborator) - the Node SDK's own `mutateStorage`/`setPresence`/`broadcastEvent` methods are the typed, in-SDK equivalent of that guide's raw REST calls, and were used instead of hand-rolled `fetch()` calls. `liveblocks.config.ts` - `Storage.flow` is now properly typed as `LiveblocksFlow<CanvasNode, CanvasEdge>` (from `@liveblocks/react-flow`, the exact type that package's own `useLiveblocksFlow` manages under its default `storageKey: "flow"` - previously an empty, unspecced `{}`), and `RoomEvent` gained a discriminated `{type: "ai-status", status, message}` variant as the shared status feed the spec asked for - both were "unused until specced" stubs left by earlier specs, not scope creep. This is a breaking change for `RoomProvider`'s `initialStorage` prop (now required since `Storage` isn't empty), so `components/editor/canvas-room.tsx` gained `initialStorage={{flow: new LiveObject({nodes: new LiveMap(), edges: new LiveMap()})}}` (`@liveblocks/client`'s `LiveObject`/`LiveMap`, mirroring exactly what `useLiveblocksFlow` would otherwise lazily create on first connect) - the one other file this spec's "only edit trigger/design-agent.ts" instruction didn't anticipate needing, but was a direct, unavoidable consequence of typing Storage correctly rather than a scope decision. `trigger/design-agent.ts` fully rewritten: an ephemeral, unauthenticated "Ghost AI" participant (`liveblocks.setPresence`, `userId: "ai-agent"`, color from the existing `getCursorColor` hash function in `lib/liveblocks.ts` reused as-is) appears in `useOthers()` exactly like a real collaborator while the task runs - meaning `components/editor/presence-avatars.tsx` and `components/editor/canvas-cursors.tsx` render its avatar/cursor/"thinking" state with **zero code changes**, confirming Liveblocks' own claim that ephemeral presence "appears in `useOthers()` hooks across connected clients" the same as a real connection. Presence clears itself via a final `setPresence` call with the API's minimum 2-second TTL (no `deletePresence` method exists) rather than lingering at its normal 45s refresh TTL. Status updates (`started`/`thinking`/`generating`/`complete`/`error`) are `liveblocks.broadcastEvent`-ed as the shared status feed at each key step - published, but no `useEventListener` consumer was added anywhere (the AI sidebar's chat UI still isn't wired to `/api/ai/design` at all, so there's nothing to attach a listener to yet; this is a real, intentional gap, not an oversight). The actual generation: `readCanvasContext` reads the room's current `flow` storage via `liveblocks.getStorageDocument(roomId, "json")` (loosely typed/cast rather than fought through `ToJson<Storage>`'s generic, since it's read-only prompt context) so Gemini can reference real existing node/edge ids for edits and avoid re-describing an already-populated canvas from scratch. `generateText` with `output: Output.object({schema})` (not the deprecated `generateObject` - `ai@7.0.66`'s own type definitions flag it `@deprecated Use generateText with an output setting instead`, caught by the IDE's live diagnostics mid-implementation, not by reading changelogs) produces a small ordered plan of 7 action kinds (`addNode`/`moveNode`/`resizeNode`/`updateNodeData`/`deleteNode`/`addEdge`/`deleteEdge`) via a Zod discriminated union - shapes are constrained to `NODE_SHAPES`, colors to an index into `NODE_COLORS` (never a raw hex value the model could hallucinate outside the palette), and move/resize actions are clamped to sane numeric ranges. New nodes deliberately carry **no** model-supplied position - `computeNewNodePositions` lays them out in a fixed 3-column grid below the canvas's existing bounding box (`LAYOUT_GAP_X`/`LAYOUT_GAP_Y` spacing), enforcing the spec's "layout and spacing rules" deterministically in code rather than hoping an LLM reasons correctly about pixel geometry - `moveNode` is the one path that still trusts model-supplied coordinates, since repositioning is inherently about explicit placement. `applyDesignPlan` resolves every action's node/edge references through an `idMap` seeded with the real ids of nodes already on the canvas, extended as each `addNode` action runs with its AI-chosen short id (`n1`, etc.) mapped to a real generated id (same `${shape}-${Date.now()}-${counter}`/`edge-${source}-${target}-${Date.now()}-${counter}` scheme as `lib/canvas.ts`'s client-side `createNodeId`/`createEdgeId`, reimplemented locally rather than imported since that file pulls in a "use client" React component module for its drag-preview helpers) - so a plan can connect a brand-new node to an existing one in the same `addEdge` action. All mutation happens inside one `liveblocks.mutateStorage(roomId, ({root}) => ...)` call against the *same* `root.get("flow").get("nodes"/"edges")` `LiveMap`s `useLiveblocksFlow` reads on the client (creating the `flow` `LiveObject` defensively if a room's Storage somehow doesn't have it yet), not a parallel storage shape - `LiveObject`'s `update` method is invariant in its generic parameter, so a freshly-`new LiveObject({...})`-constructed value (whose shape TypeScript infers narrowly from just the fields supplied) never structurally satisfies `LiveMap.set`'s expected type (the full, mostly-optional shape `LiveblocksNode`/`LiveblocksEdge` generate from React Flow's own `Node`/`Edge` interfaces, meant for reading storage back out, not hand-constructing writes) - resolved by extracting each map's exact expected value type via its own `.get()` return type and casting the constructed plain object to it at each `.set()` call site, rather than fighting the generic inference field-by-field. Errors are caught around the whole run, broadcast as an `"error"` status event, then rethrown so Trigger.dev's own retry/failure handling still applies; AI presence is cleared in a `finally` block so it disappears on both success and failure paths. `GOOGLE_AI_API_KEY` (the spec's named env var, read via `createGoogle({apiKey: process.env.GOOGLE_AI_API_KEY})` rather than the provider's own default `GOOGLE_GENERATIVE_AI_API_KEY` env var name) was **not** actually already in `.env.local` as the spec claimed - no `.env.local` file existed at all; added one with a placeholder for the user to fill in with a real key from Google AI Studio, same pattern as `LIVEBLOCKS_SECRET_KEY`/`TRIGGER_SECRET_KEY` before it. `zod` was promoted from an already-installed transitive dependency to a direct one (`npm install zod@4.4.3`, matching the version already in `node_modules`) since `design-agent.ts` now imports it directly. Verified: `next build` (`tsc`, not just bundling) and `eslint .` both clean; `npx trigger.dev@latest dev` confirmed the local worker still builds both `hello-world` and `design-agent` with no bundling/registration errors (the actual Gemini call and Liveblocks mutation weren't exercised end-to-end - that needs a real `GOOGLE_AI_API_KEY` and a live room, neither available in this pass).
- 22-design-agent-api.md: Trigger.dev itself was already fully installed and configured in this project before this spec landed (`@trigger.dev/sdk`/`@trigger.dev/build`, `trigger.config.ts` at the root pointing at project `proj_ztpwafybogkruyjkwoms` via a `TRIGGER_PROJECT_REF` env var loaded through an explicit `import "dotenv/config"` since the CLI's config resolution runs outside Next's own env pipeline, `dirs: ["trigger"]` after moving the scaffolded `example.ts` from `src/trigger/` to a root-level `trigger/` directory) — this spec only added the design-generation-specific pieces on top of that existing setup, per its own step 4 instruction to "reuse the existing setup instead of creating a new pattern." New `prisma/models/task-run.prisma` — `model TaskRun` (`runId String @unique` - Postgres/Prisma already indexes a unique column, so no separate redundant `@@index([runId])` was added despite the spec listing it as a separate bullet; `projectId`, `userId`, `createdAt`, `@@index([userId, projectId])` compound index as specced); `prisma migrate dev --name add_task_run` applied cleanly against the live database. New `trigger/design-agent.ts` — `designAgentTask` (`id: "design-agent"`), takes `{prompt, roomId}`, `logger.log`s the input, returns it unchanged - no AI provider calls, no node/edge generation, per the explicit scope limits. New `app/api/ai/design/route.ts` - `POST`, follows the exact `getCurrentIdentity`/`checkProjectAccess` gate pattern every prior project-scoped route already uses (401 signed-out, 403 no access to `projectId`), validates the body (`prompt` non-empty, `roomId`/`projectId` strings) with the same `isXBody` type-guard convention `canvas/route.ts` established, triggers the task via `tasks.trigger<typeof designAgentTask>("design-agent", ...)` with a type-only import of the task (per the "never import the task instance into backend code" rule from the trigger-authoring-tasks skill), writes a `TaskRun` row keyed by the returned run id, and returns `{runId}`. New `app/api/ai/design/token/route.ts` - `POST`, looks up the `TaskRun` by the submitted `runId` and checks `taskRun.userId === identity.userId` (ownership is the run creator, not general project access - the run itself already only got created once project access was confirmed at trigger time) before minting a token via `auth.createPublicToken({scopes: {read: {runs: [taskRun.runId]}}})` scoped to that one run only, returning `{token}`; 403 for a missing run or a mismatched owner (same not-found-vs-unauthorized non-disclosure pattern `checkProjectAccess` already uses elsewhere). Neither route is wired into the AI sidebar's UI yet - this spec was backend-only, per its own scope limits. Verified: `npx prisma generate` regenerated the client (needed for `prisma.taskRun` to type-check), `tsc --noEmit` (via `next build`), `eslint .`, `next build` all clean, and `npx trigger.dev@latest dev` run twice (once after adding `design-agent.ts`, once after the full route wiring) confirmed the local worker builds both `hello-world` and `design-agent` cleanly with no registration errors.
- 21-canvas-autosave.md: installed `@vercel/blob` (`^2.8.0`). Step 1 needed no schema migration - `prisma/models/project.prisma`'s `canvasJsonPath String?` was already added preemptively back in 05-prisma.md's own words ("optional canvasJsonPath for future canvas blob storage"), reused verbatim. New `app/api/projects/[projectId]/canvas/route.ts` - `PUT`/`GET`, both gated the same way `/api/liveblocks-auth` already gates room access (`getCurrentIdentity`/`checkProjectAccess` from `lib/project-access.ts` - owner or collaborator, 401/403), matching the "anyone with room access can save/load" model rather than owner-only like rename/delete. `PUT` uploads to a **stable** pathname (`projects/[projectId]/canvas.json`, `addRandomSuffix: false, allowOverwrite: true`) so every save overwrites the same blob instead of orphaning old snapshots, then writes the returned URL to `canvasJsonPath`; `GET` reads that URL from Prisma, then the blob content, falling back to `{nodes: [], edges: []}` if there's no saved canvas yet or the fetch fails. This project's Blob store turned out to be **private-access-only** - `access: "public"` (my first attempt) was rejected outright by a real upload call, not something visible from the SDK's types alone; switched to `access: "private"` on `put()`, and `GET` had to switch from a plain `fetch(url)` to `@vercel/blob`'s own `get(url, {access: "private"})` (returns a `ReadableStream`, read via `new Response(stream).json()`) since a private blob's URL alone isn't enough to authenticate a plain fetch. New `hooks/use-canvas-autosave.ts` - `useCanvasAutosave({projectId, nodes, edges, enabled})`, a 1500ms debounce (`setTimeout`, cleared/reset on every `nodes`/`edges` change) around a `PUT` call, tracking `status: "idle" | "saving" | "saved" | "error"`; also returns `saveNow` (the same save logic, bypassing the debounce) for the navbar's manual Save button. New `hooks/use-canvas-load.ts` - `useCanvasLoad({projectId, nodes, edges, onNodesChange, onEdgesChange})`, a `hasRunRef`-guarded one-time-only effect that checks the room's current `nodes`/`edges` length before ever calling `GET` - non-empty means active collaboration (or another client's load already ran) and the fetch is skipped entirely, satisfying "saved canvas does not load if the room already has active nodes or edges"; empty triggers the `GET` and adds the result via the same `onNodesChange([{type: "add", item}])` batching pattern `handleImportTemplate` already uses. `components/editor/canvas.tsx` wires `isLoadComplete` (from the load hook) as the autosave hook's `enabled` flag, so a save can never fire before the load check has resolved one way or the other; a new `onAutosaveStateChange` prop reports `{status, saveNow}` up through `canvas-room.tsx` to `editor-shell.tsx`, which stores `status` in state (re-renders the button) and `saveNow` in a ref (doesn't need to) - the inverse-direction counterpart to how `isTemplatesModalOpen` already flows the other way, needed because the hooks (and the `nodes`/`edges` they read) only exist inside `CanvasRoom`'s Liveblocks subtree, while the button has to live in the navbar outside it. New `components/editor/save-status-button.tsx` - `SaveStatusButton`, a single navbar button whose icon/label *is* the status indicator (`Cloud`/`Save` idle, spinning `Loader2`/`Saving…`, `Check`/`Saved`, `AlertCircle`/`Retry` in `text-error`) and whose `onClick` calls `saveNow` - doubling as both the spec's "Save button" and its "status indicator" rather than two separate elements. Verified beyond the "Check When Done" list: a throwaway `tsx` script (`--env-file=.env`, removed after) exercised the exact `put`/`get`/Prisma-update sequence the route uses against the *real* Blob store and database - confirmed a private upload succeeds, the stored URL round-trips through Prisma, `get()` returns byte-identical JSON back, and a second `put()` to the same pathname genuinely overwrites in place (same URL, no orphaned duplicate) - this is what caught the public-vs-private access mismatch in the first place, before it could reach the actual route. Separately, a temporary `/dev-preview` route (removed after, with its one-line `proxy.ts` addition) rendered the two hooks directly with Playwright intercepting `/api/projects/.../canvas` (`page.route()`, no real auth needed since this tests client-side hook logic in isolation) - confirmed an empty room loads a mocked 2-node saved snapshot while a seeded/non-empty room never issues the `GET` at all; confirmed the debounce genuinely debounces (no `PUT` at 500ms, exactly one at ~1800ms) and coalesces three rapid changes into two saves rather than four; confirmed manual "save now" bypasses the debounce and a `500` response flips status to `error`. That harness also caught a real bug in `use-canvas-load.ts`: the original implementation returned a `cancelled`-flag cleanup function from the effect, which is the standard pattern for aborting stale async work on unmount - but combined with the `hasRunRef` once-only guard, React StrictMode's dev-only double-invoke (mount → fake cleanup → real mount) let the *fake* cleanup set `cancelled = true` on the one fetch that was actually still in flight (the ref survives the fake unmount, so the second, real effect invocation just no-ops instead of starting a fresh un-cancelled fetch), permanently preventing the load from ever completing in development. Fixed by dropping the cleanup-based cancellation entirely - `hasRunRef` already makes the fetch start exactly once for the component's whole lifetime, and setting state after a genuine unmount is a harmless no-op in modern React, so there was nothing left for the cancellation flag to actually protect against. `tsc --noEmit` (via `next build`), `eslint .`, `next build` all clean.

## In Progress

- None yet.

## Next Up

- No feature spec currently sitting unimplemented in `context/feature-specs/`. Both AI flows are now fully functional end to end: design generation (submit → trigger → live run tracking → canvas updates via Liveblocks) and spec generation (backend flow + Blob/Prisma persistence + sidebar list/preview/download all exist and are wired together). Waiting on a new spec for: a full custom color picker beyond the fixed `NODE_COLORS` pairs (15/16-node-color-toolbar.md), custom/saved user templates and server-persisted templates (18-starter-templates.md), and wiring the Specs tab's "Generate Spec" button itself - nothing yet assembles the current `chatHistory`/`nodes`/`edges` and calls `POST /api/ai/spec` from the UI, so specs can only be created by triggering the task some other way (e.g. directly via the Trigger.dev dashboard) until that's specced.

## Open Questions

- Add unresolved product or implementation questions here.

## Architecture Decisions

- Add decisions that affect the system design or data model.

## Session Notes

- 23-design-agent-logic.md follow-up (two real runtime bugs, caught by the user actually running `trigger dev`, not by build/lint): (1) `setPresence`/`broadcastEvent`/`mutateStorage` all 404'd with a blank-message error against a room nobody had opened yet, since Liveblocks rooms in this app are only created lazily on first auth (`app/api/liveblocks-auth/route.ts`) - fixed by calling the same `liveblocks.getOrCreateRoom(roomId, {defaultAccesses: ["room:write"]})` at the top of the task, and separately improved the catch block to log a `LiveblocksError`'s `status`/`details` (previously only `.message`, which can come back empty) so a REST-level failure doesn't show as a bare `Error:` in the run log again. (2) A real Gemini call failed with `AI_NoObjectGeneratedError: response did not match schema` - root cause was `z.discriminatedUnion` for the 7 action kinds, which Zod compiles to a JSON Schema `oneOf` that Gemini's structured-output schema support doesn't reliably honor; fixed by flattening `designActionSchema` into one object (every field optional except `type`) with a new `normalizeAction()` doing the "which fields are required for this type" check in code instead of in the schema Gemini has to satisfy - confirmed via `ai-sdk.dev`'s current docs that `generateText` + `Output.object` (not the user's initial suspicion) is in fact the currently-recommended API and does forward the schema to the provider's native structured-output mechanism, same as the deprecated `generateObject` would have. Also switched from a made-up `GOOGLE_AI_API_KEY` env var to `@ai-sdk/google`'s actual default `GOOGLE_GENERATIVE_AI_API_KEY` (which the user had already set, in both `.env` and `.env.local`) and simplified to importing the default `google` provider instance instead of wrapping it with `createGoogle`. `tsc`/`eslint`/`trigger dev` all reverified clean after each fix.

- `context/current-issues.md` bug fixes (4 issues, fixed and cleared from the file one at a time): (1) `components/editor/shape-panel.tsx` buttons were `draggable` but keyboard-unreachable in practice — added an `onAddShape` prop wired to each button's `onClick`, and `components/editor/canvas.tsx` now shares one `addNode(shape, position, width, height)` helper between `handleDrop` and a new `handleAddShape` that computes the drop position itself: `wrapperRef.current.getBoundingClientRect()`'s center run through `screenToFlowPosition`, offset by half the shape's default size so the node lands centered in the visible viewport instead of top-left-anchored there. (2) `lib/collaborators.ts`'s `listCollaborators` called Clerk's `getUserList` with every collaborator email in one request, but that endpoint caps `emailAddress` at 100 values — added a `chunk()` helper and `Promise.all` over 100-email batches, merging the results before building `byEmail`; now correct for projects with >100 collaborators. (3) `components/editor/canvas-node.tsx`'s 4 `Handle`s had no `id`, so React Flow couldn't distinguish multiple handles of the same type on one node — added stable ids (`top-target`/`right-source`/`bottom-target`/`left-source`) matching the position+type of each. (4) same `listCollaborators` — the Clerk enrichment call ran un-guarded after the Postgres write in the `POST` collaborator route, so a Clerk-side failure (outage or bad response) surfaced as a request failure even though the invitation itself had already succeeded, and a retry would then hit the `(projectId, email)` unique constraint and 409; wrapped the Clerk batch-lookup section in `try/catch` so a failure there just leaves `name`/`imageUrl` as `null` for every collaborator instead of throwing - `POST`/`GET` both stay available through a Clerk outage now. Verified: `tsc --noEmit`, `eslint .`, `next build` all clean after each of the 4 fixes.
- `context/current-issues.md` bug fixes on `components/editor/project-sidebar.tsx`: (1) the closed sidebar used `aria-hidden` but its `<Link>`s, close button, and tab triggers stayed reachable by keyboard since `aria-hidden` alone doesn't remove elements from the tab order — added `inert={!isOpen}` to the `<aside>` (React 19 supports `inert` as a native DOM prop), which correctly disables focus/interaction on the whole subtree when closed; verified with a temporary `/dev-preview` route + Playwright that `Tab` from before the sidebar skips over it entirely while closed (`aside.inert === true`) and correctly reaches "Close sidebar" → tab triggers → the project link once open (`aside.inert === false`). (2) long project names could overflow the row or collide with the row's action menu because `truncate` doesn't override a flex item's automatic minimum width — added `min-w-0 flex-1` to both the owned- and shared-list project `<Link>`s (the fix was pasted into `context/current-issues.md` as an exact diff hunk, applied verbatim to both occurrences); verified visually with a screenshot showing a long name ellipsizing cleanly inside the row instead of pushing past it. `tsc --noEmit`, `eslint .`, `next build` all clean.
- 02-editor.md contained several literal typos that were corrected rather than followed verbatim: file paths `components/editor/editor-nafbar.tsx` and `component/editor/product-projects-sidebar.tsx` → `components/editor/editor-navbar.tsx` and `components/editor/project-sidebar.tsx`; icon name `PanelLeftCLose` → `PanelLeftClose` (verified against the installed `lucide-react` exports). All functional requirements (sections, tabs, buttons, slide-in behaviour, dialog token pattern) were implemented as written.
- 03-auth.md said to "define public routes using the existing sign-in and sign-up env vars" but `.env` only had the publishable/secret keys — `NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in` and `NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up` were added since those are Clerk's own conventional var names (not invented), which the spec separately requires ("do not rename or invent new ones").
- `context/current-issues.md` bug fix: dialog titles (and, latently, every other `text-base` usage — `components/ui/card.tsx` titles, `components/ui/input.tsx`, `components/ui/textarea.tsx`) rendered near-black instead of `text-copy-primary`. Root cause: `app/globals.css` registered `--color-base: var(--bg-base)` in `@theme inline` purely to get a `bg-base` utility for the page background, but Tailwind v4 auto-generates a same-named color utility for *every* class suffix from a `--color-*` token — so it silently redefined the built-in `text-base` font-size utility as `color: var(--bg-base)` (near-black) instead. Fixed by dropping `--color-base` from the theme block and hand-defining `@utility bg-base { background-color: var(--bg-base); }` instead, so `bg-base` still works per `context/ui-context.md` without clobbering `text-base`/`border-base`. Verified via a temporary public `/dev-preview` route (removed after) rendering `ProjectDialogs` and a Playwright screenshot/computed-style check confirming `dialog-title` now resolves to `rgb(240,240,244)` at 16px; `tsc --noEmit`, `eslint .`, `next build` all clean.
- `context/current-issues.md` bug fix (round 2): `submitCreate`/`submitRename` in `hooks/use-project-dialogs.ts` only checked `name.trim()`, so a name like `"!!!"` (no letters/digits) passed validation but `slugify()` collapsed it to an empty string, storing a project with `slug: ""`. Fixed by deriving `slug = slugify(name)` in both submit functions and guarding on `!slug` too, not just `!name.trim()`. `components/projects/project-dialogs.tsx` now surfaces the same distinction in the UI for both Create and Rename: the confirm button is disabled whenever the name is empty *or* the derived slug is empty, and the helper text under the input switches between "Enter a name to preview the slug" / "Name must include at least one letter or number" (shown in `text-error`) / the live `/slug` preview. Verified with the same temporary `/dev-preview` route + Playwright: typing `"!!!"` shows the red validation message and leaves the confirm button `disabled`; `tsc --noEmit`, `eslint .`, `next build` all clean.
- 07-wire-editor-home.md doesn't mention a workspace route file, but its own "Check When Done" requires "create navigates to workspace" and "delete redirects to `/editor` if deleting the active workspace" - neither is a real, verifiable behavior without somewhere for "the new workspace" to actually be, so `app/editor/[projectId]/page.tsx` was added as a minimal placeholder (reuses `EditorShell`, shows the project name, no canvas) rather than treated as out of scope.
- 06-project-apis.md required editing `proxy.ts` even though the spec only asked for API routes: `clerkMiddleware`'s `auth.protect()` redirects to `/sign-in` for *any* unauthenticated request, including fetch/XHR calls to `/api/*` - there is no way for a route handler's own `auth()` check to return `401` JSON if the middleware already redirected before the handler ran. Adding `/api(.*)` to the public-route matcher lets middleware skip `protect()` for API routes so each handler's own `if (!userId) return 401` is what actually runs; this was required to satisfy the spec's explicit "unauthenticated requests return `401`" check, not a scope decision made independently.
- 05-prisma.md: `prisma.config.ts` already pointed `schema` at the `prisma/` folder (multi-file schema support), so `prisma/models/project.prisma` was picked up automatically alongside the existing `prisma/schema.prisma` (datasource/generator only) - confirmed with `prisma validate`. Added `enum ProjectStatus { DRAFT ARCHIVED }`, `model Project` (`ownerId` for the Clerk user id, `name`, optional `description`, `status ProjectStatus @default(DRAFT)`, optional `canvasJsonPath` for future canvas blob storage, `createdAt`/`updatedAt`, `@@index([ownerId])` + `@@index([createdAt])`, plus the `id` Prisma requires), and `model ProjectCollaborator` (`projectId` relation with `onDelete: Cascade`, `email`, `createdAt`, `@@unique([projectId, email])`, `@@index([email])`, `@@index([projectId, createdAt])`). Ran `prisma migrate dev --name add_project_models` against the real `DATABASE_URL` (Prisma Postgres, direct connection, not `prisma+postgres://`) - applied cleanly, then `prisma generate` regenerated `app/generated/prisma`. `lib/prisma.ts` is the cached singleton the spec asked for: branches on whether `DATABASE_URL` starts with `prisma+postgres://` (passes `accelerateUrl` straight to `PrismaClient` - Prisma 7's client accepts that natively, no `@prisma/extension-accelerate` package needed) vs. everything else (`new PrismaClient({ adapter: new PrismaPg(databaseUrl) })`), and caches the instance on `globalThis` outside production so dev hot-reloads reuse one connection. Verified beyond the "Check When Done" list: a throwaway `tsx` script exercised the singleton's pattern end-to-end (create a `Project` with a nested `ProjectCollaborator`, confirm the row, delete the `Project`, confirm the collaborator cascade-deleted) against the live database, then was removed. `tsc --noEmit`, `eslint .`, `next build` all clean.
