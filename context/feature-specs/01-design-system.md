Read the agent file before starting. 

We are adding design system and UI primitive components. 

Install and configure `shadcn/ui`.
Add the following ShadCN components.
Button, card, dialogue, input, tab text areas and scrollarea.
Do not modify the files after installation. The folder is components/ui/*.

Also install Lucid React. Create a `lib/utils` With the reusable `cn()` Helper for merging Tailwind classes.

Ensure all components match the dark theme in `global.css`. 

### Check when Done
- All components import without errors
- 'cn()` works properly
- No default light styling appears